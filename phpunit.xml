<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="unit">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
            <!-- includes/ is where all new backend code goes (src/ is frozen), so it
                 belongs in the coverage scope too. Without it the numbers describe
                 only the one directory nobody is allowed to add to. -->
            <directory>includes</directory>
        </include>
    </source>
</phpunit>
