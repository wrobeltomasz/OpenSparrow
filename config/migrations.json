{
    "3.0": {
        "removed_files": [],
        "deprecated_files": [],
        "removed_config_keys": [],
        "notes": "Initial public baseline release. The full system-table schema is created by the single 3.0_baseline database migration (Admin → Migrations → Initialize System Tables); there is no earlier release to upgrade from, so no files or config keys are removed. Subsequent releases append their own file/config cleanup entries below this one."
    },
    "3.1": {
        "removed_files": [],
        "deprecated_files": [],
        "removed_config_keys": [],
        "notes": "Added per-table record image galleries with grid thumbnails and edit-form upload. Extended the automation webhook action into a full outbound integration path for n8n / Make / custom receivers: the After delete trigger now fires (rules see the pre-delete row snapshot), PATCH and DELETE methods were added, the JSON envelope carries old_data on update and delete, custom request headers are supported for receiver-side auth, and failed sends can retry once or twice on timeouts and 5xx/429. Webhook signing secrets and custom header values are now stored encrypted (secret_enc / headers_enc) and are never returned to the browser; rules saved earlier keep working from their plaintext secret / headers fields and are re-encrypted on the next save, so no configuration keys are removed. Fixed JavaScript cache busting across both the admin panel and the frontend: an import map now gives every ES module in each tree the same version, so after an upgrade the browser refetches all of them instead of only the entry files. Previously any change to a non-entry module stayed invisible until the user manually hard-refreshed. Adds the 3.1_table_comments database migration, which applies COMMENT ON TABLE / COMMENT ON COLUMN descriptions to every spw_* table (metadata only, no structural change) - run Admin -> Migrations -> Initialize System Tables to apply it. Note reminders (User menu -> Notes) now take a date and time instead of a date only; the 3.1_notes_reminder_time migration widens spw_notes.reminder_date from date to timestamp (existing reminders keep their day at 00:00) and the notification cron delivers a reminder once that moment has passed."
    }
}
