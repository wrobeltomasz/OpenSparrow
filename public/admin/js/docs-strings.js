// This file is part of OpenSparrow - https://opensparrow.org
// SPDX-License-Identifier: LGPL-3.0-or-later
// Copyright (C) 2024-2026 OpenSparrow Contributors
// Licensed under LGPL v3. See COPYING.LESSER file for details.

// admin/js/docs-strings.js — Localized documentation strings (STRINGS) for the admin docs page, keyed by language (en/pl). Data only; consumed by docs.js.
export const STRINGS = {
    en: {
        sidebarTitle: 'Contents',
        title: 'OpenSparrow - Admin Panel Documentation',
        subtitle: 'Configure your frontend application, manage database connections, and build dynamic dashboards, calendars and workflows without writing a single line of code.',
        s0_head: '0. First-Run Setup',
        s0_warn_strong: 'Fresh installation?',
        s0_warn_text: 'The admin panel automatically detects that the database is not yet configured (no connection or missing <code>spw_users</code> table) and opens without requiring a login. Follow these steps exactly:',
        s0_step1: 'Open <code>/admin</code> in your browser — the panel loads in setup mode with a yellow banner.',
        s0_step2: 'Go to <strong>System → Database</strong>, enter host, port, database name, user and password, then click <strong>Save config</strong>.',
        s0_step3: 'Go to <strong>System → Migrations</strong> and click <strong>Apply Pending Migrations</strong>. This creates all <code>spw_*</code> tables and inserts a default admin account: username <code>admin</code>, password <code>admin</code>.',
        s0_step4: 'Go to <code>/login</code>, log in as <code>admin</code> / <code>admin</code>. You are redirected to <code>/admin</code> automatically.',
        s0_step5: 'Go to <strong>System → Users</strong>, find the <em>admin</em> row and click <strong>Change pwd</strong>. Enter your current password (<code>admin</code>) and set a strong new one.',
        s0_after: 'From that point the admin panel requires a valid session — the setup bypass is permanently closed once <code>spw_users</code> exists in the database.',
        s0b_head: '0b. Admin Panel Layout',
        s0b_desc: 'The admin panel uses a collapsible left sidebar with five sections (Overview, Data Management, Workflows, Knowledge Base, System). The header provides global actions.',
        s0b_data_label: 'Data Management',
        s0b_data: 'Board, Calendar, CSV Import, Dashboard, ETL, Files, Printouts, Schema, User Records, Views. Add Table, Menu Preview, Schema Map and M2M Builder are inner tabs of Schema, not separate sidebar items.',
        s0b_workflows_label: 'Workflows',
        s0b_workflows: 'Automations, Workflow Manager.',
        s0b_kb_label: 'Knowledge Base',
        s0b_kb: 'RAG Documents.',
        s0b_system_label: 'System',
        s0b_system: 'Anonymization, Backup Tables, Cron Notifications, Demo Systems, Health Check, Migrations, Performance, Settings, Users. Audit &amp; Snapshots is an inner tab of Settings.',
        s0b_save_label: 'Save config',
        s0b_save: 'Persists the currently edited configuration to the <code>spw_config</code> store in the database — except <code>database</code> and <code>security</code>, which stay files on disk. After a successful save a green status pill appears next to the button confirming which configuration was written. Error pills stay visible for 6 seconds so they are not missed.',
        s0b_guard_label: 'Unsaved-changes guard',
        s0b_guard: 'Tracks pending changes in config-editing tabs (Schema, Dashboard, Calendar, etc.) and shows a confirmation prompt before discarding them. Tabs that save immediately via API (Users, Database, Health, Backup) never trigger this warning.',
        s0b_debug_label: 'Debug FE mode',
        s0b_debug: 'Toggle in the header. When enabled, the frontend exposes a <code>#debug</code> panel with raw payloads for schema/API responses — useful when building new tables or troubleshooting grids.',
        s0b_docs_label: 'Docs icon (book)',
        s0b_docs: 'Opens this documentation page.',
        s1_head: '1. Technical Requirements &amp; Database Structure',
        s1_desc: 'Before configuring OpenSparrow, make sure your PostgreSQL database meets these core requirements:',
        s1_pk_label: 'Primary keys (mandatory)',
        s1_pk: 'Every table <strong>must</strong> have a primary key column named <code>id</code> (typically <code>SERIAL</code> or <code>BIGSERIAL</code>). OpenSparrow relies on this exact column name to edit, delete, and view specific records.',
        s1_fk_label: 'Foreign keys (relationships)',
        s1_fk: 'Use standard PostgreSQL foreign keys to link tables. The UI detects them automatically. Recommended naming convention: <code>table_name_id</code> (e.g. <code>company_id</code>).',
        s1_enum_label: 'ENUM types',
        s1_enum: 'Custom PostgreSQL ENUM types are fully supported and rendered as <code>&lt;select&gt;</code> menus in the frontend.',
        s1_bool_label: 'Boolean types',
        s1_bool: 'Boolean columns render as switch toggles in edit forms and as dropdown filters in data grids.',
        s1_schema_label: 'System schema',
        s1_schema: 'OpenSparrow stores its internal tables (<code>spw_*</code>) in a dedicated PostgreSQL schema (default: <code>app</code>). The schema is resolved in this order: <code>schema</code> key in <code>config/database.json</code>, then the <code>PGSCHEMA</code> environment variable, then the <code>app</code> fallback.',
        s1_systables_head: 'System tables (<code>spw_*</code> prefix)',
        s1_systables_desc: 'OpenSparrow reserves a set of internal tables, all prefixed with <code>spw_</code> and created inside the configured system schema:',
        s1_t_users: 'frontend user accounts (id, username, password hash, role, active flag, optional contact details).',
        s1_t_users_log: 'audit trail of user actions (LOGIN, LOGOUT, CRUD operations).',
        s1_t_notifications: 'per-user in-app notifications, produced by the cron runner.',
        s1_t_notifications_log: 'execution log for the notifications cron: start/end time, status (<code>running</code> / <code>success</code> / <code>error</code>), trigger source (<code>cron</code> or <code>admin</code>), number of sources processed and notifications created, and error message on failure.',
        s1_t_files: 'metadata for files uploaded through the Files module.',
        s1_t_login_attempts: 'rolling log used by the DB-backed rate limiter on <code>login.php</code> (IP-hash and username counters).',
        s1_t_comments: 'user comments attached to any record. Each row links to a specific record via <code>related_table</code> + <code>related_id</code>, stores the author (<code>user_id</code>), body text (max 4000 chars), and a soft-delete timestamp.',
        s1_t_snapshots: 'JSONB snapshots of records captured after every INSERT or UPDATE. Each row is linked to the corresponding <code>spw_users_log</code> entry via <code>log_id</code> (CASCADE DELETE). Only active when the Record Snapshots module is enabled (see section 9b).',
        s1_t_owners: 'append-only ownership log. Each row records who owns a specific record (<code>table_name</code> + <code>record_id</code>) and who made the change (<code>changed_by</code>). The current owner is the row where <code>is_current = true</code>; all previous rows form the full ownership history. Created automatically on INSERT; reassignable by editors via the Record Owner panel in the edit view (see section 9c).',
        s1_t_migrations: 'migration tracker. One row per applied migration name + timestamp. Bootstrapped automatically by the first run of <em>Apply Pending Migrations</em>. Used by <em>System → Migrations</em> to determine which schema changes have already been applied.',
        s1_t_config: 'the application configuration store — one JSONB row per key (schema, menu, settings, dashboard, calendar, board, workflows, automations, views, files, print, anonymization, user_records, user_policy, user_table_access, rag, etl, etl_flows, clickstats), versioned with optimistic locking.',
        s1_t_config_log: 'full change history of the configuration store: the previous value of each key, who changed it and when.',
        s1_t_notes: 'private user notes, optionally linked to a record and carrying a reminder date delivered by the notifications cron.',
        s1_t_rag: 'knowledge base storage: documents (<code>spw_rag_files</code>), their full-text chunks (<code>spw_rag_chunks</code>) and the query history (<code>spw_rag_queries</code>, <code>spw_rag_query_sources</code>).',
        s1_t_automations: 'execution history of automation rules (<code>spw_automation_runs</code>) and the outgoing e-mail queue (<code>spw_automation_emails</code>).',
        s1_t_imports: 'CSV import runs (<code>spw_imports</code>) with per-row detail in <code>spw_import_rows_log</code>.',
        s1_t_etl: 'ETL job run history (<code>spw_etl_log</code>) plus flow runs and their per-step detail (<code>spw_etl_flow_run_log</code>, <code>spw_etl_flow_step_log</code>).',
        s1_t_anon: 'anonymization run history (<code>spw_anonymization_log</code>) and the generated GDPR/EDPB compliance reports (<code>spw_anonymization_report</code>).',
        s1_t_release_migrations: 'release migrations already applied from <code>config/migrations.json</code> — one row per version.',
        s1_note_strong: 'Note:',
        s1_note_text: 'Tables starting with <code>spw_</code> are treated as system tables. They are <strong>filtered out</strong> from the <em>Sync DB Tables</em> list in the Schema tab and will not appear in your application schema, even if they live in the same PostgreSQL schema as your business tables.',
        s2_head: '2. Schema &amp; Grid Configuration',
        s2_desc: 'The <strong>Schema</strong> tab is the core of your configuration. It maps your database tables to frontend grids and forms.',
        s2_addtable_head: 'Add Table (Data Management → Add Table)',
        s2_addtable_desc: 'The dedicated <strong>Add Table</strong> tab creates a new physical PostgreSQL table and optionally registers it in <code>spw_config.schema</code> — no manual sync step required.',
        s2_name_label: 'Table Name &amp; Database Schema',
        s2_name: 'Lowercase identifiers only. The <code>id serial PRIMARY KEY</code> column is always added automatically.',
        s2_display_label: 'Display Name',
        s2_display: 'Auto-filled from the table name (underscores → spaces, title-case). Used as the label in menus and headings when registered in <code>spw_config.schema</code>.',
        s2_presets_label: 'Column Presets',
        s2_presets: 'Check <em>Timestamps</em> to automatically add <code>created_at</code> and <code>updated_at</code> columns (<code>timestamp DEFAULT now() NOT NULL</code>).',
        s2_columns_label: 'Per-column options',
        s2_columns: '<code>varchar(255)</code>, <code>text</code>, <code>int4</code>, <code>int8</code>, <code>boolean</code>, <code>date</code>, <code>timestamp</code>. Not Null adds a <code>NOT NULL</code> constraint. Index: <code>btree</code>, <code>hash</code>, or <code>unique</code>. Comment stored as <code>COMMENT ON COLUMN</code>.',
        s2_register_label: 'Register in app schema',
        s2_register: '(checked by default): After the table and all columns are created in the database, the table entry is written to <code>spw_config.schema</code> automatically.',
        s2_addcol_label: 'Add new columns (Schema tab)',
        s2_addcol: 'Inside an existing table\'s configuration, click <em>+ Add Column</em> to append a physical column to the database.',
        s2_synctables_label: 'Sync DB Tables',
        s2_synctables: 'Fetches all tables from the connected database and merges them into your schema configuration. System tables with the <code>spw_</code> prefix are skipped automatically.',
        s2_synccols_label: 'Sync Columns from DB',
        s2_synccols: 'Inside a table\'s configuration, fetches all columns for that table and adds any that are missing. Also reads PostgreSQL <code>COMMENT ON COLUMN</code> values for descriptions.',
        s2_coldesc_label: 'Column Description (tooltip)',
        s2_coldesc: 'Appears as a native browser tooltip when hovering over column headers in the data grid.',
        s2_preview_label: 'Live sidebar preview',
        s2_preview: 'Updates in real time when editing Display Name, Icon, or Hide flag.',
        s2_typemap_label: 'Smart type mapping',
        s2_typemap: 'Native PostgreSQL types are mapped to clean frontend types (Text, Number, Date, Boolean, Enum).',
        s2_remove_label: 'Remove tables',
        s2_remove: 'Removes a table from JSON configuration only — does not drop the physical table.',
        s2_fksearch_label: 'Foreign-key search &amp; display',
        s2_fksearch: 'Assign multiple display columns to a foreign key. The frontend renders them as searchable inputs.',
        s2_visibility_label: 'Visibility &amp; ordering',
        s2_visibility: 'Toggle per-column visibility in the grid and reorder with Up/Down arrows.',
        s2_validation_label: 'Validation rules (regex)',
        s2_validation: 'Enforce strict formats with regular expressions.',
        regex_phone: 'Phone (9-15 digits, optional +)',
        regex_postal: 'Postal code (XX-XXX)',
        regex_username: 'Username (3-16 chars)',
        regex_price: 'Price / decimal',
        regex_date: 'Date (YYYY-MM-DD)',
        regex_password: 'Strong password',
        s2_subtables_head: 'Subtables (one-to-many relationships)',
        s2_subtables_desc: '<strong>+ Add Subtable</strong> displays related child records inside the parent record\'s detail view.',
        s2_sub_open: 'Open the Schema editor of the parent table and click <em>+ Add Subtable</em>.',
        s2_sub_target_label: 'Target table',
        s2_sub_target: 'the child table to display.',
        s2_sub_fkcol_label: 'Foreign-key column',
        s2_sub_fkcol: 'the column in the child table referencing the parent\'s <code>id</code>.',
        s2_img_head: 'Record images',
        s2_img_desc: 'Per-table image galleries, enabled per table on the Schema tab. Each record can hold multiple photos, uploaded from its edit form and previewed as thumbnails in the grid.',
        s2_img_enable_label: 'Enable',
        s2_img_enable: 'toggle images on for the table; the config is stored alongside the table\'s schema (<code>tables[table].images</code>).',
        s2_img_max_label: 'Max per record',
        s2_img_max: 'limit between 1 and 50 images per record.',
        s2_img_grid_label: 'Show in grid',
        s2_img_grid: 'when on, the grid shows a thumbnail column (up to 4 previews) with a hover popup for larger previews; click opens the full image in a new tab.',
        s2_img_upload_label: 'Upload',
        s2_img_upload: 'images are uploaded and deleted from an Images tab on the record\'s edit form (not available on create — save the record first); a workflow step whose table has a gallery also offers an upload field, attaching the photo once that step\'s record is saved. They use the same storage and size/extension limits as the Files module.',
        s3_head: '3. Dashboard Builder',
        s3_desc: 'The <strong>Dashboard</strong> tab composes analytical views. The layout uses a fixed <strong>3-column grid</strong>; each widget occupies 1, 2, or 3 columns.',
        s3_types_head: 'Widget types',
        s3_stat_label: 'Stat Card',
        s3_stat: 'Colored tile showing a single aggregate number (COUNT, SUM, or AVG) of a selected table/column.',
        s3_bar_label: 'Bar Chart (Horizontal / Vertical)',
        s3_bar: 'Groups rows by a chosen column. Each bar is clickable.',
        s3_pie_label: 'Pie Chart',
        s3_pie: 'Same group/aggregate model, rendered as a conic-gradient pie. Each slice is clickable.',
        s3_line_label: 'Line Chart (Time Series)',
        s3_line: 'Plots a value over time instead of grouping by a category. Configure a time axis column (X), a granularity (Day / Week / Month / Year), an aggregation column (Y) and its function (Count / Sum / Average). An optional <em>Fill area under line</em> checkbox shades the area below the line.',
        s3_list_label: 'Data List',
        s3_list: 'Top-N rows ordered by a selected column.',
        s3_props_head: 'Widget proportions',
        s3_width_label: 'Width',
        s3_height_label: 'Height',
        s3_height: 'Small (140 px), Medium (280 px), Large (440 px).',
        s3_mobile: 'On mobile screens all widgets collapse to a single column.',
        s3_period_head: 'Global period filter and trends',
        s3_period_desc: 'The dashboard header offers a period selector (All time / Today / Last 7 days / Last 30 days / This month). It filters every widget by its first date column; count and sum cards additionally show a percentage change versus the equally long previous period.',
        s3_export_desc: 'Users with export permission see a CSV button on each widget (on hover) that downloads the currently displayed data — no server round-trip, respects the active period filter.',
        s3_filter_head: 'Filter Conditions (WHERE)',
        s3_filter_desc: 'Every widget supports structured filter conditions combined with AND/OR. Column names are validated against the schema; values are escaped.',
        s3_filter_ops: 'Available operators',
        s3_preview_head: 'Live Preview',
        s3_preview_desc: 'A <strong>Live Preview</strong> panel renders the actual widget with sample data and updates automatically. Uses fixed mock data — no database connection required.',
        s3_global_head: 'Global settings',
        s3_global_desc: 'Controls dashboard menu entry and grid gap. Defaults to <code>20px</code>.',
        s4_head: '4. Calendar Module',
        s4_desc: 'The <strong>Calendar</strong> tab binds date columns from your database to a visual calendar.',
        s4_sources_label: 'Data sources',
        s4_sources: 'Overlay multiple tables on a single calendar.',
        s4_color_label: 'Color coding',
        s4_color: 'Assign a color per source.',
        s4_context_label: 'Row context',
        s4_context: 'The full database row is attached to each calendar event.',
        s4b_head: '4b. Board (Kanban) Module',
        s4b_desc: 'The <strong>Board</strong> tab turns a table into a Kanban board: records become cards laid out in lanes, one lane per value of a status column. Dragging a card to another lane instantly updates that column.',
        s4b_multi_label: 'Multiple boards',
        s4b_multi: 'The tab holds a named list of boards (<code>spw_config.board</code>, the <code>boards</code> array), not a single one. Each board is configured independently and gets its own frontend menu entry, so one installation can run several boards over different tables.',
        s4b_table_label: 'Source table',
        s4b_table: 'Per board, select the one table whose records are shown as cards.',
        s4b_status_label: 'Status column',
        s4b_status: 'The most important setting — its values become the board lanes. An <code>enum</code> column is recommended so lanes and their colors come from <code>enum_colors</code>; for non-enum columns lanes are derived from the existing distinct values.',
        s4b_cards_label: 'Card content',
        s4b_cards: 'Choose the title column and optional detail fields shown on every card; foreign keys appear as their display label.',
        s4b_dnd_label: 'Drag &amp; drop',
        s4b_dnd: 'Available to the editor role. Moves are validated against the allowed value set, respect record ownership, and are audit-logged (<code>BOARD_MOVE</code>).',
        s5_head: '5. Workflows Builder',
        s5_desc: 'The <strong>Workflows</strong> tab composes multi-step wizards guiding users through structured data entry across related tables.',
        s5_steps_label: 'Steps setup',
        s5_steps: 'Add sequential steps and select a target table per step.',
        s5_link_label: 'Relational linking',
        s5_link: 'Link child records to parents by selecting the foreign-key column.',
        s5_multi_label: 'Multiple records',
        s5_multi: 'Enable <em>Allow adding multiple records</em> per step.',
        s5_validation_head: 'Step Validation',
        s5_validation_desc: 'A workflow cannot be saved if any step is missing a <strong>name</strong> or <strong>target table</strong>, or if the workflow has no steps at all. Incomplete steps are highlighted with a red left border and "— incomplete" label in the editor. The Save button is blocked until all steps are complete.',
        s5_proc_head: 'Stored Procedure on Next Step',
        s5_proc_desc: 'Any step can call a PostgreSQL <strong>procedure</strong> when the user clicks <em>Next step</em>. Pick the schema and procedure from a dropdown listing the database\'s stored procedures; one row then appears per declared <code>IN</code> parameter, each fed either from a workflow field (choose the source step and column — the current step or any earlier one) or from a fixed value typed in by hand. The procedure runs <strong>before</strong> the wizard advances and receives only form values, never record ids — nothing is written to the database until the final review screen. If the procedure raises an exception, its message is shown to the user and the wizard stays on the current step, so a procedure can act as a server-side validation gate. Configuration is stored per step under <code>steps[].procedure</code>.',
        s6_head: '6. Users Management',
        s6_desc: 'The <strong>Users</strong> tab (<em>System → Users</em>) manages all accounts.',
        s6_roles_label: 'Roles',
        s6_admin: 'access to this admin panel only. Cannot log in to the frontend.',
        s6_editor: 'full CRUD access to the frontend. Cannot access the admin panel.',
        s6_viewer: 'read-only access to the frontend. Cannot access the admin panel.',
        s6_pwd_label: 'Change password',
        s6_pwd: 'Own account requires current password. Other accounts: admin override, no current password needed.',
        s6_status_label: 'Active status',
        s6_status: 'Toggle Active / Inactive to revoke or restore login access.',
        s7_head: '7. Database Configuration',
        s7_desc: 'Manage the core PostgreSQL connection from <strong>System → Database</strong>.',
        s7_schema_label: 'System Schema',
        s7_schema: 'Sets the PostgreSQL schema used for all <code>spw_*</code> tables. Defaults to <code>app</code>.',
        s7_test_label: 'Test Saved Connection',
        s7_test: 'Always click <em>Save config</em> first — the test reads persisted <code>database.json</code>, not in-form values.',
        s7_login_label: 'Login protection',
        s7_login: 'DB-backed rate limiter, CSRF tokens, session fingerprinting, 8-hour session lifetime, <code>SameSite=Lax</code> / <code>HttpOnly</code> cookies.',
        s8_head: '8. Backup Tables',
        s8_desc: '<strong>System → Backup Tables</strong> creates timestamped copies of selected tables directly in PostgreSQL.',
        s8_format_label: 'Backup name format',
        s8_what_label: 'What is copied',
        s8_what: 'Column structure and all data rows. Indexes and constraints are <strong>not</strong> copied.',
        s9_head: '9. System Health, Cron &amp; Config',
        s9_migrations_label: 'Database Migrations',
        s9_migrations: '<em>System → Migrations</em>. Click <strong>Apply Pending Migrations</strong> to run all unapplied schema changes.',
        s9_diagnostics_label: 'System diagnostics',
        s9_diagnostics: 'Live checks for PHP version, extensions, write permissions, and DB connectivity.',
        s9_cron_label: 'Run Notifications Cron',
        s9_cron: 'Executes <code>cron/cron_notifications.php</code> ad-hoc. Each run is recorded in <code>spw_users_notifications_log</code>.',
        s9b_head: '9b. Audit &amp; Record Snapshots',
        s9b_desc: '<strong>Settings → Audit &amp; Snapshots</strong> (an inner tab of the Settings tab, not a separate sidebar item) controls the record snapshot module — captures full record state after every write.',
        s9b_how_label: 'How it works',
        s9b_how: 'Every INSERT and UPDATE saves a JSONB copy to <code>spw_record_snapshots</code>, linked to <code>spw_users_log</code> via <code>log_id</code>.',
        s9b_toggle_label: 'Toggle',
        s9b_toggle: 'Writes to <code>spw_config.settings</code>. Takes effect on the next request. Overridden by <code>RECORD_SNAPSHOTS_ENABLED</code> env var.',
        s9b_storage_label: 'Storage growth',
        s9b_storage: 'Monitor table size and implement a retention policy for high-volume installations.',
        s9c_head: '9c. Database Migrations',
        s9c_desc: '<strong>System → Migrations</strong> applies and tracks all schema changes to <code>spw_*</code> system tables.',
        s9c_applied: 'Applied migrations are recorded in <code>spw_migrations</code>. Re-running is always safe.',
        s9c_adding_label: 'Adding migrations (developers)',
        s9c_adding: 'Append to the <code>$migrations</code> array in <code>includes/admin/migrations.php</code> (<code>init_db</code> action) and mirror the new key in the <code>$known</code> list in the same file and in <code>$knownMig</code> in <code>includes/admin/overview.php</code>. Never modify existing entries.',
        s9d_head: '9d. Record Ownership',
        s9d_desc: 'Every record can have an <strong>owner</strong> tracked in <code>spw_record_owners</code>.',
        s9d_auto_label: 'Auto-assignment',
        s9d_auto: 'Creator is automatically set as owner on INSERT.',
        s9d_change_label: 'Changing the owner',
        s9d_change: 'Editor/Admin role: select user and click <em>Change Owner</em> in the Record History tab.',
        s9d_history_label: 'Full history',
        s9d_history: 'Every change appends a row. No data is deleted.',
        s9o_head: '9o. Per-User Frontend Access',
        s9o_desc: 'The <strong>Users → Access</strong> tab restricts a frontend user to a subset of the schema tables, the configured views, the print templates, the boards and the workflows. The groups are independent, and all of it is independent of the role: a Viewer limited to two tables still sees those two read-only.',
        s9o_where_label: 'Where it is stored',
        s9o_where: 'The <code>user_table_access</code> key in <code>spw_config</code>, shaped as <code>{"users": {"&lt;id&gt;": {"tables": [], "views": [], "prints": [], "boards": [], "workflows": []}}}</code>. Views and printouts are granted by name, boards and workflows by their id — none of those configs binds to a single table the way a grid page does.',
        s9o_empty_label: 'Empty selection',
        s9o_empty: 'In any group it means <strong>no restriction</strong> for that group, not "no access" — every existing account keeps full access until you tick entries. To cut a user off entirely, deactivate the account instead.',
        s9o_admin_label: 'Admin accounts',
        s9o_admin: 'Never restricted and not listed in the tab — they work in this panel and must see the whole schema.',
        s9o_scope_label: 'What it covers',
        s9o_scope: 'Menu, grid, create/edit pages, mass edit, data cleanup, files, RAG context, dashboard widgets, calendar sources, boards, the Views page and the Printouts page. Subtable tabs in <code>edit.php</code> are filtered too, because a tab shows whole rows of the child table. Enforcement is server-side on every endpoint; hiding menu entries is only cosmetic.',
        s9o_hidden_label: 'Hidden helper tables',
        s9o_hidden: 'Tables marked <em>hidden</em> have no menu entry and no grid of their own, so they are not listed in the tab. They are granted automatically together with the table whose subtable they are, transitively — otherwise restricting a user would silently remove subtable tabs nobody could give back. The Tables group names them under the list as you tick parents. Visible subtables are never granted automatically: those stay your explicit choice.',
        s9p_head: '9p. User Contact Details',
        s9p_desc: 'Each account can carry a first name, last name, email address and phone number. The data is informational only — it is not used for login, notifications or any frontend display.',
        s9p_where_label: 'Where',
        s9p_where: 'Set them in <strong>Users → Manage Users</strong>, either in the "Add New User" form or via the "Edit Details" button on a row. They are shown in the Name / Email / Phone columns.',
        s9p_optional_label: 'Optional',
        s9p_optional: 'All four fields may stay empty (an empty cell renders as a dash). The email is only checked for a valid format and is not required to be unique.',
        s9p_scope_label: 'Scope',
        s9p_scope: 'Admin panel only. Nothing on the frontend reads these columns, and they are never exposed through the public API.',
        s9o_limits_label: 'Known limits',
        s9o_bindings_label: 'Boards and workflows still need their tables',
        s9o_bindings: 'Granting a board or a workflow does not grant the tables it uses — the two are ticked separately and both have to hold. A board whose table you did not grant is not listed, and a workflow is dropped when any of its steps targets a table the user cannot reach. So when you grant one of these, grant its tables as well, or the entry simply will not appear. This is on purpose: a board or a workflow is a way of looking at data, never a way around who may see it.',
        s9o_views_label: 'Views and printouts read past the boundary',
        s9o_views: 'A view or printout a user may open can select from tables they have no access to. The query lives in the configuration and is not derived from the table grants, so granting the view grants everything that view reads — table access does not filter inside it. Treat the two as separate decisions: before granting a view or a print template, check what it actually selects. If one mixes data meant for different audiences, split it into per-audience views rather than expecting table access to mask part of it.',
        s9o_limits: 'Foreign-key labels still resolve across the boundary — a permitted table may display a name from a restricted one. That is the line: a foreign key exposes a name, a subtable exposes rows, which is why subtables are filtered and FK labels are not.',
        s9e_head: '9e. Grid Default Sort &amp; Load Limit',
        s9e_sort_label: 'Default Sort Order',
        s9e_sort: 'One or more sort rules (column + ASC/DESC). Fallback is <code>id DESC</code>.',
        s9e_limit_label: 'Initial Load Limit',
        s9e_limit: 'Max rows on first load. <code>0</code> means unlimited.',
        s9e_stored_label: 'Stored in',
        s9e_stored: 'as <code>"default_sort"</code> and <code>"initial_limit"</code>.',
        s9f_head: '9f. Grid Drilldown — Quick Add',
        s9f_desc: 'Subtable block headers show a <strong>+</strong> button that navigates to <code>create.php</code> pre-filling the foreign key. Visible to Editor and Admin only.',
        s9f2_head: '9f2. Grid Action Buttons',
        s9f2_desc: 'The last grid column holds a <code>more_vert.png</code> overflow button; clicking it opens a small panel with the row actions. Opening one row\'s panel closes any other.',
        s9f2_edit: '<code>edit_square.png</code> icon, navigates to <code>edit.php</code>.',
        s9f2_dup: '<code>content_copy.png</code> icon, clones the row via <code>mass_duplicate</code> and reloads the grid. No confirmation.',
        s9f2_delete: '<code>delete.png</code> icon (red hover), requires confirmation.',
        s9f2_visible: 'The whole actions column is rendered only for roles that may write — in practice the <strong>Editor</strong> role, since Admin accounts are redirected from the frontend to the admin panel. Viewers get a read-only grid with no actions column.',
        s9g_head: '9g. Performance Tab',
        s9g_desc: '<strong>Admin → System → Performance</strong> — read-only diagnostic panel with six independent sections.',
        s9g_li1_label: 'Missing Index Advisor',
        s9g_li1: 'Finds columns lacking indexes. Generates ready-to-run <code>CREATE INDEX</code> SQL.',
        s9g_li2_label: 'Unused Indexes',
        s9g_li2: 'Queries indexes with <code>idx_scan = 0</code>. Generates <code>DROP INDEX</code> SQL.',
        s9g_li3_label: 'Slow Query Analyzer',
        s9g_li3: 'Reads <code>pg_stat_statements</code>. Highlights queries over 100 ms / 500 ms.',
        s9g_li4_label: 'Table Statistics &amp; Bloat',
        s9g_li4: 'Dead rows, bloat %, scan counts, last vacuum timestamps.',
        s9g_li5_label: 'Database Health',
        s9g_li5: 'Cache hit ratio, connections, deadlocks, DB size, PG version.',
        s9g_li6_label: 'Schema Configuration Warnings',
        s9g_li6: 'Flags tables &gt;20 columns, missing limits, missing sort, subtables without columns_to_show.',
        s9h_head: '9h. Cron Notifications Tab',
        s9h_desc: '<strong>Admin → System → Cron Notifications</strong> — six-section management interface for the notification cron.',
        s9h_li1_label: 'Manual Run',
        s9h_li1: 'Execute <code>cron/cron_notifications.php</code> immediately.',
        s9h_li2_label: 'Run History',
        s9h_li2: 'Last 50 entries from <code>spw_users_notifications_log</code>.',
        s9h_li3_label: 'Notification Stats',
        s9h_li3: 'Total, unread, due today, upcoming. Top 10 users by unread count.',
        s9h_li4_label: 'Cron Setup',
        s9h_li4: 'Ready-to-copy commands for Linux/macOS, Windows Task Scheduler, Docker.',
        s9h_li5_label: 'Log Cleanup',
        s9h_li5: 'Purge <code>spw_users_notifications_log</code> rows older than N days.',
        s9h_li6_label: 'Email',
        s9h_li6: '"From" address for queued automation emails, plus optional authenticated SMTP delivery (host, port, encryption, username, password) with a "Test SMTP Connection" button. The password is stored encrypted and never shown back in the UI.',
        s9i_head: '9i. Grid Page Size',
        s9i_admin_label: 'Admin default',
        s9i_admin: 'Schema tab → <em>Global Grid Settings</em> → <em>Default Page Size</em> (10 / 25 / 50 / 100).',
        s9i_user_label: 'User override',
        s9i_user: '<em>Rows per page</em> selector in the pagination bar, saved to <code>localStorage</code>.',
        s9i_priority_label: 'Priority',
        s9i_fallback: 'fallback',
        s9j_head: '9j. Many-to-Many Relationships',
        s9j_desc: 'M2M relationships link records across tables via a <strong>junction table</strong>. Rendered as a checkbox panel in edit/create forms.',
        s9j_how_head: 'How it works end-to-end',
        s9j_how1_label: 'Create the junction table in PostgreSQL',
        s9j_how1: 'with two FK columns and a UNIQUE constraint.',
        s9j_how2_label: 'Configure the relationship',
        s9j_how2: 'Schema → parent table → <em>Many-to-Many Relationships</em> → <strong>+ Add Many-to-Many</strong>.',
        s9j_how3_label: 'Save config',
        s9j_how3: '— the checkbox panel appears automatically.',
        s9j_config_head: 'Admin configuration fields',
        s9j_config_li: 'Display Label, Junction Table, Self FK, Other FK, Other Table, Display Column.',
        s9j_runtime_head: 'Runtime behaviour',
        s9j_runtime1: 'On Save: all existing junction rows deleted and new rows inserted atomically in a single PostgreSQL transaction.',
        s9j_runtime2: 'Viewer role sees checkboxes in disabled state.',
        s9k_head: '9k. Schema Map (ERD)',
        s9k_desc: '<strong>Data Management → Schema → Schema Map</strong> renders an interactive ERD from <code>spw_config.schema</code>. Uses force-directed auto-layout. No external libraries.',
        s9k_types_label: 'Connection types',
        s9k_types: 'Foreign key (solid grey), Subtable (dashed navy), Many-to-many (dotted grey) — matching the on-canvas legend. A counter above the canvas reports how many tables and how many links of each kind were drawn.',
        s9k_controls_label: 'Controls',
        s9k_controls: 'Pan, zoom, drag tables, click to highlight, search, show/hide hidden tables, Fit View, Export PNG.',
        s10_head: '10. Files Module',
        s10_desc: 'Central repository for documents and media backed by <code>spw_files</code>. The tab is both a file browser and the module\'s configuration screen.',
        s10_browse_label: 'Browsing',
        s10_browse: 'Paginated list (25 rows per page by default, <code>FILES_PAGE_LIMIT</code>) with full-text search over name, display name and tags, a file-type filter, and click-to-sort on type, name, display name, tags, size, related record and upload date.',
        s10_meta_label: 'Per-file metadata',
        s10_meta: 'Display name and tags are editable in place. Deletion is a <strong>soft delete</strong> — the row keeps its <code>deleted_at</code> stamp and drops out of every listing rather than being removed.',
        s10_bulk_label: 'Bulk actions',
        s10_bulk: 'Select several files to delete them in one call or to apply tags to all of them at once; a single request is capped at 500 files.',
        s10_config_label: 'Configuration',
        s10_config: 'Max file size, allowed types, allowed extensions, storage path (must not be web-accessible — downloads streamed through <code>file_download.php</code>). The admin panel saves this config verbatim, so the extension list is intersected server-side with the set of extensions the uploader can actually content-verify — an entry that is not verifiable is rejected at upload no matter what the config says.',
        s10_relations_label: 'Record relations',
        s10_relations: 'Optionally link uploads to specific rows in a target table.',
        s10b_head: '10b. Views Editor',
        s10b_desc: '<strong>Data Management → Views</strong> exposes read-only PostgreSQL views as browsable grid pages, alongside regular tables. Both ordinary and <strong>materialized</strong> views are discovered and configured identically.',
        s10b_matview_label: 'Materialized views',
        s10b_matview: 'A materialized view serves the rows stored at its last refresh, not live data — OpenSparrow only reads it and never refreshes it for you. Keep it current with <code>REFRESH MATERIALIZED VIEW schema.name</code> from a database job or a cron entry, or the page will quietly show stale figures. Sync lists only relations the application\'s database user may <code>SELECT</code>, so a view it cannot read is never offered.',
        s10b_sync_label: 'Sync',
        s10b_sync: '"↻ Sync PostgreSQL Views" discovers views in the searched schema(s) and their column metadata; discovered views are merged into <code>spw_config.views</code> without discarding existing per-view settings (icon, menu name, drill-down).',
        s10b_schemas_label: 'Schemas tab',
        s10b_schemas: 'By default sync only searches the application schema (<code>config/database.json</code> → <code>schema</code> / <code>PGSCHEMA</code>, default <code>app</code>). Check additional schemas (queried from <code>information_schema.schemata</code>) to include views living elsewhere, e.g. a demo schema. The selection is saved with the views configuration.',
        s10b_display_label: 'Per-view configuration',
        s10b_display: 'Display name, menu name, icon, visibility, and drill-down levels — configured the same way as for tables.',
        s10b_readonly_label: 'Read-only',
        s10b_readonly: 'Views cannot be edited or deleted through the grid — they are read-only, like native SQL views.',
        s11_head: '11. Menu Preview &amp; Navigation Editor',
        s11_desc: 'Renders the frontend sidebar and lets you rearrange or nest items by dragging. Every change is saved automatically.',
        s11_dnd_head: 'Drag &amp; drop controls',
        s11_reorder_label: 'Reorder',
        s11_reorder: 'Drag above or below another item.',
        s11_nest_label: 'Nest',
        s11_nest: 'Drop onto the middle zone of a top-level item. Maximum depth: <strong>1 level</strong>.',
        s11_unnest_label: 'Un-nest',
        s11_unnest: 'Drag a child item to the top level.',
        s11_autosave_label: 'Auto-save',
        s11_autosave: 'Every drop triggers a save to <code>spw_config.menu</code> after 350 ms debounce.',
        s11b_head: '11b. Demo Systems (Quick-Start Templates)',
        s11b_desc: '<strong>System → Demo Systems</strong> provides a pre-built demo application: CRM.',
        s11b_what_label: 'What gets installed',
        s11b_what: 'PostgreSQL schema, seed data, app schema entries, dashboard widgets, calendar sources, workflows, SQL views.',
        s11b_safety_label: 'Safety',
        s11b_safety: 'Both install and uninstall require typing <code>CONFIRM</code>.',
        s11b_cleanup_label: 'Cleanup on uninstall',
        s11b_cleanup: 'Demo schema dropped (CASCADE). Configuration entries cleaned if they contain only demo content.',
        s11b_demo1_head: 'Demo 1: CRM',
        s11b_demo1_text: 'Companies, contacts, deals, quotes, invoices, assets, activities. FK subtables, deal stage color coding, revenue drill-down by year → month, Kanban deals board, automations (incl. welcome email), GDPR anonymization rules for stale leads.',
        s11c_head: '11c. CSV Import',
        s11c_desc: 'Data Management → CSV Import — bulk-imports rows from a .csv file into any schema-configured table. A memory-safe PHP generator reads the file line-by-line; inserts are batched in transactions of up to 1 000 rows.',
        s11c_step1_label: 'Step 1 — Select table &amp; upload',
        s11c_step1: 'Pick the target table from the dropdown, then drag &amp; drop (or click) to upload a .csv file (max 50 MB). The file is validated server-side by real MIME type, not just extension.',
        s11c_parse_label: 'Delimiter &amp; encoding',
        s11c_parse: 'Delimiter: comma, semicolon, tab (TSV) or pipe. Encoding: UTF-8, Windows-1250, Windows-1252, ISO-8859-1, ISO-8859-2 or Windows-1251. Both are remembered in <code>localStorage</code> between imports, and the local preview re-parses with them.',
        s11c_create_label: 'Create new table from CSV',
        s11c_create: 'Instead of picking an existing table, tick <em>Create new table from CSV</em>: column types are proposed from the file\'s sample values, you name the table and pick its schema, and the table is created and registered before the rows are loaded. The typed name is normalized to lowercase with non-alphanumerics folded to underscores.',
        s11c_mode_label: 'Import modes',
        s11c_mode: '<strong>Normal mode</strong> — batched INSERT in transactions of up to 1 000 rows; tracks per-row errors and supports upsert. <strong>Fast COPY mode</strong> — streams the file straight to PostgreSQL via <code>COPY FROM STDIN</code>, 10-60x faster on large files, but there is <strong>no per-row error tracking</strong> (one type mismatch rolls back the whole import) and <strong>no upsert</strong> — the conflict column is hidden while it is selected.',
        s11c_step2_label: 'Step 2 — Map columns',
        s11c_step2: 'Each CSV header is listed with sample values. Use the dropdown to map it to a database column, or leave "Skip" to ignore it. Auto-mapping pre-selects columns whose names match case-insensitively.',
        s11c_upsert_label: 'Upsert / conflict handling',
        s11c_upsert: 'Select a conflict column (e.g. <code>id</code>). On conflict the matched row is updated instead of rejected. The column must have a UNIQUE constraint and must be included in the column mapping.',
        s11c_types_label: 'Type casting',
        s11c_types: 'Values are cast before insert: dates accept <code>dd.mm.yyyy</code>, <code>dd/mm/yyyy</code>, <code>yyyy-mm-dd</code>; booleans accept <code>true/false/1/0/yes/y/t</code>; numbers strip thousand-separator commas.',
        s11c_errors_label: 'Error handling',
        s11c_errors: 'Row-level cast failures and DB batch rollbacks are both caught. Skipped rows are written to <code>spw_import_rows_log</code>; the import continues. Status is <code>failed</code> only if every row was skipped.',
        s11c_tables_label: 'System tables',
        s11c_tables: '<code>spw_imports</code> — one row per import run (user, file, table, status, counters). <code>spw_import_rows_log</code> — per-skipped-row detail (row number, raw JSON, error message). Activated by <em>Apply Pending Migrations</em>.',
        s11c_history_label: 'Import history',
        s11c_history: 'The bottom of the page lists the last 100 imports. Click <em>Log</em> on any row with skipped records to expand the per-row error table inline.',
        s11d_head: '11d. ETL Import',
        s11d_desc: '<strong>Data Management → ETL</strong> — scheduled or on-demand batch import from an external source database (MySQL, MariaDB, PostgreSQL, SQLite) or a CSV file fetched over FTP/FTPS, into a local PostgreSQL target table. Independent of the removed MySQL Gateway feature; connects via a driver-based PDO layer, extensible to Oracle/DB2/SQL Server.',
        s11d_conn_label: 'Sources',
        s11d_conn: 'Define one or more named sources. Database sources (MySQL, MariaDB, PostgreSQL) take host, port, database, user and password; SQLite takes a readable file path instead of a host; the <em>CSV file (FTP/FTPS)</em> source takes protocol, host, remote directory, file name, delimiter, header flag and passive mode. <em>Test connection</em> verifies reachability before saving. Stored in <code>spw_config.etl</code>; the password is masked (<code>********</code>) once saved and only replaced when re-entered.',
        s11d_jobs_label: 'Jobs',
        s11d_jobs: 'Each job picks which source it reads from, and defines a target table plus an optional column map (source → target; auto-matched by name when omitted). Database-source jobs also carry a source query (a single read-only <code>SELECT</code>/<code>WITH</code> — DML/DDL and multiple statements are rejected); CSV/FTP-source jobs have no query and import the whole file.',
        s11d_modes_label: 'Load modes',
        s11d_modes: '<strong>Full refresh</strong> (truncate + insert), <strong>Append</strong> (insert only), <strong>Upsert</strong> (insert with <code>ON CONFLICT</code> update on one or more key columns).',
        s11d_incremental_label: 'Incremental loading',
        s11d_incremental: 'Set an incremental column and use the <code>{{watermark}}</code> placeholder in the source query; the engine substitutes the last-seen value (or the configured initial value) and stores the new high-water mark after each successful run.',
        s11d_preview_label: 'Preview',
        s11d_preview: 'Runs the source query against the configured connection and shows up to 20 sample rows and columns before saving a job.',
        s11d_schedule_label: 'Schedule',
        s11d_schedule: '<em>Manual</em>, <em>Daily</em>, <em>Weekly</em> or <em>Monthly</em>, enforced by <code>cron/cron_etl.php</code>. <em>Run Now</em> triggers all enabled jobs immediately regardless of schedule.',
        s11d_log_label: 'Run history',
        s11d_log: 'The last 50 runs from <code>spw_etl_log</code> show rows read/written, duration and any error. Log Cleanup purges entries older than N days.',
        s11e_head: '11e. ETL Flows',
        s11e_desc: '<strong>Data Management → ETL → Flows</strong> tab — chains existing ETL jobs into an ordered sequence (start, job, job, ..., end tiles). Steps reference job ids from the Jobs tab, so editing a job also changes what any flow referencing it does. Stored in <code>spw_config.etl_flows</code>, separate from the <code>etl</code> key.',
        s11e_run_label: 'Execution',
        s11e_run: 'Steps run strictly in order and the flow stops immediately at the first failing step — remaining steps do not run. <em>Run Now</em> executes a flow immediately, bypassing its schedule.',
        s11e_schedule_label: 'Schedule',
        s11e_schedule: 'Same <em>Manual</em>/<em>Daily</em>/<em>Weekly</em>/<em>Monthly</em> model as jobs, enforced by <code>cron/cron_etl_flow.php</code>, guarded per flow (not module-wide).',
        s11e_log_label: 'Run history',
        s11e_log: 'Each flow shows its own run history: one row per flow run in <code>spw_etl_flow_run_log</code> (overall status, which step failed) plus per-step detail in <code>spw_etl_flow_step_log</code>.',
        s13_head: '13. Multilingual / i18n Module',
        s13_desc: 'OpenSparrow supports multiple UI languages via flat JSON translation files. The active language is resolved per session; both PHP templates and all JS modules share the same bundle.',
        s13_config_head: 'Configuration — spw_config.settings',
        s13_config1: '<code>"default_language": "pl"</code> — locale used when no session preference is stored.',
        s13_config2: '<code>"available_languages": ["en", "pl"]</code> — enables the language switcher; lists accepted locale codes.',
        s13_trans_head: 'Translation files',
        s13_trans_desc: 'Each locale has one file: <code>languages/{locale}.json</code> (e.g. <code>languages/pl.json</code>). Structure:',
        s13_trans1: 'Top-level keys are namespaces: <code>common</code>, <code>grid</code>, <code>form</code>, <code>auth</code>, <code>header</code>, <code>admin</code>, <code>pagination</code>, <code>filter</code>, <code>files</code>, <code>images</code>, <code>notifications</code>, <code>notes</code>, <code>dashboard</code>, <code>workflow</code>, <code>comments</code>, <code>owners</code>, <code>views</code>, <code>calendar</code>, <code>board</code>, <code>print</code>, <code>data_cleanup</code>, <code>mass_edit</code>, <code>mass_owner</code>, <code>mass_duplicate</code>, <code>mass_delete</code>, <code>shortcuts</code>, <code>agent</code>, <code>setup</code>. One extra top-level key, <code>_meta</code>, is not a namespace: it carries the locale\'s own <code>name</code> and text <code>dir</code>.',
        s13_trans2: 'Plural values are objects: <code>{"one": "...", "few": "...", "many": "..."}</code> for Polish; <code>{"one": "...", "other": "..."}</code> for English.',
        s13_trans3: 'Variable placeholders use <code>{name}</code> syntax: e.g. <code>"showing": "Showing {from}–{to} of {total} records"</code>.',
        s13_trans4_label: 'Critical',
        s13_trans4: 'All double-quote characters inside JSON string values must be escaped as <code>\\"</code>. An unescaped <code>"</code> silently breaks <code>json_decode</code>, causing the entire locale to fall back to English with no error shown.',
        s13_php_head: 'PHP API',
        s13_php1: '<code>includes/i18n.php</code> — loaded via <code>includes/session.php</code>, available everywhere.',
        s13_php2: '<code>I18n::locale()</code> — returns active locale string (e.g. <code>\'pl\'</code>). Use in <code>&lt;html lang="..."&gt;</code>.',
        s13_php3: '<code>t($key, $vars = [], $count = null)</code> — translates a dot-notation key; replaces <code>{name}</code> placeholders; selects plural form when <code>$count</code> is provided.',
        s13_php4: 'Bundle served to JS via <code>api.php?action=i18n_bundle</code> as a flat key→value JSON object.',
        s13_js_head: 'JavaScript API',
        s13_js1: '<code>assets/js/i18n.js</code> — ES module singleton. Import: <code>import { I18n } from \'./i18n.js\';</code>',
        s13_js2: 'Always call <code>await I18n.load()</code> before rendering any translated text — widgets that render before <code>load()</code> resolves will show raw key strings.',
        s13_js3: '<code>I18n.t(\'common.save\')</code> — basic lookup.',
        s13_js4: '<code>I18n.t(\'grid.showing\', { from: 1, to: 10, total: 42 })</code> — with variable substitution.',
        s13_js5: '<code>I18n.t(\'files.count\', { count: 3 }, 3)</code> — with plural selection.',
        s13_js6: 'Build DOM nodes with <code>el.textContent = I18n.t(...)</code> — never inject translations via <code>innerHTML</code>.',
        s13_add_head: 'Adding a new language',
        s13_add1: 'Copy <code>languages/en.json</code> → <code>languages/{locale}.json</code>.',
        s13_add2: 'Translate all values; update plural forms to match the target language rules.',
        s13_add3: 'Add the locale code to <code>"available_languages"</code> in <code>spw_config.settings</code>.',
        s13_add4: 'Validate JSON before deploying: <code>node -e "JSON.parse(require(\'fs\').readFileSync(\'languages/{locale}.json\',\'utf8\'))"</code>.',
        s13_docslang_label: 'Documentation Languages',
        s13_docslang: 'The application UI is translated into all locales present in <code>languages/</code>, but this admin documentation is maintained in English and Polish only — the language bar at the top of this page offers EN and PL. Do not add further documentation locales; keep both versions in sync when a section changes.',
        s12_head: '12. Deployment Notes',
        s12_li1: '<strong>Serve only <code>public/</code>:</strong> set the web server document root to the <code>public/</code> directory. All backend code, configuration and storage live above it and cannot be reached over HTTP; the per-folder <code>.htaccess</code> <code>Deny from all</code> rules ship as defense-in-depth.',
        s12_li2: '<strong>Storage permissions:</strong> <code>config/</code> and <code>storage/</code> must be writable by the web-server user.',
        s12_li3: '<strong>Backups:</strong> Keep regular <code>pg_dump</code> snapshots — they cover configuration too, since it lives in <code>spw_config</code>.',
        s12_li4: '<strong>Demo mode:</strong> Set <code>DEMO_MODE=true</code> to block all write operations in the admin API.',
        s12_env_head: 'Environment variables',
        s12_th_var: 'Variable',
        s12_th_default: 'Default',
        s12_th_desc: 'Description',
        env_appenv: 'Runtime environment.',
        env_dbhost: 'PostgreSQL host.',
        env_dbport: 'PostgreSQL port.',
        env_timezone: 'IANA timezone for every PostgreSQL session.',
        env_cookies: 'Set <code>false</code> on plain HTTP.',
        env_session: 'Hard session expiry in seconds (8 h).',
        env_none: 'none',
        env_iphash_req: 'Required in production.',
        env_iphash: 'HMAC secret for IP pseudonymisation.',
        env_ip_attempts: 'Failed login threshold per IP.',
        env_user_attempts: 'Failed login threshold per username.',
        env_lockout: 'Lockout window in minutes.',
        env_demo: 'Block all write operations in admin API.',
        env_files: 'Default upload size limit.',
        env_snapshots: 'Enable record snapshots system-wide.',
        env_pgschema: 'Schema for <code>spw_*</code> tables.',
        env_autogen: 'auto-generated',
        env_enckey: 'Symmetric key encrypting secrets at rest in <code>spw_config</code> (webhook signing secrets, SMTP password, Ollama Cloud API key). When unset, a random key is generated once and persisted to <code>includes/.secret_key</code>. <strong>Set it explicitly on any deployment where that file is not stable</strong> — containers or multiple app nodes each generating their own key cannot decrypt each other\'s stored secrets.',
        env_proxy: 'Whether <code>X-Forwarded-For</code> is trusted when resolving the client IP for login throttling. Set to <code>false</code> when the app is reachable directly, so a spoofed header cannot dodge the rate limiter.',
        env_samesite: 'SameSite policy for the session cookie.',
        env_savepath: 'Session storage directory. Falls back to the project\'s <code>storage/sessions</code> when unset.',
        env_hsts: 'Max-age of the HSTS header, in seconds.',
        env_mailfrom: '"From" address for queued automation emails; also settable from the Cron Notifications tab. Email delivery stays disabled until one of the two is set.',
        s12_env_note: 'This table covers the variables that matter most when deploying. Other tunables exist (page-size caps, cache max-ages, RAG rate limits, thumbnail sizing); each is read through <code>get_env()</code> in <code>includes/config.php</code>, which is the authoritative list.',
        sRag_head: '15. RAG (Retrieval-Augmented Generation)',
        sRag_desc: 'Local knowledge base powered by Ollama. Upload .txt documents and query them with AI-generated answers grounded in your data.',
        s13_docs_label: 'Upload Documents',
        s13_docs: 'Navigate to <strong>Admin → RAG Knowledge Base → Documents</strong>. Upload .txt files, tag them by topic (e.g. <em>legal</em>, <em>faq</em>), and optionally assign a language. Documents are stored in <code>spw_rag_files</code> and indexed for full-text search.',
        s13_config_label: 'Configure Ollama',
        s13_config: 'Go to <strong>Settings tab</strong>. Choose your Ollama setup: <ul style="padding-left:20px;margin-top:8px;"><li><strong>Local Ollama:</strong> URL <code>http://localhost:11434</code> (default). Pull and run models on your machine.</li><li><strong>Ollama Cloud:</strong> URL <code>https://api.ollama.com</code>. Requires an API key from your Ollama Cloud account. Paste it in the API Key field.</li></ul> Select a model from the list. Click <strong>Test &amp; load models</strong> to verify connection and display available models.',
        s13_test_label: 'Test Queries',
        s13_test: 'Use the <strong>Test tab</strong> to send questions against your knowledge base. Optionally filter by tag or select a response language. The result shows the generated answer and which documents were matched.',
        s13_stats_label: 'Statistics',
        s13_stats: 'View query history, token counts, and response times in the <strong>Statistics tab</strong>. Track Ollama performance and document relevance over time.',
        s13_multilang_label: 'Multilingual Support',
        s13_memory_label: 'Conversation Memory',
        s13_memory: 'Set <strong>Conversation memory (turns)</strong> in the <strong>Settings tab</strong> (0–10, default <code>0</code> = off). The front-end Ask AI panel sends back the previous question and the previous answer, so follow-up questions ("and for last month?") keep their context. Only the last turn is sent — never the whole thread — and the server trims it to the configured limit. The model is told to use it for one purpose only: working out what an elliptical question refers to, never as a source of facts. A refusal is never remembered (feeding "not in the context" back in only primes the next refusal), and the <em>Clear</em> button drops the memory outright.',
        s13_multilang: 'Queries automatically respond in the user\'s current UI language. Optionally tag documents with language codes (e.g. <em>lang:pl</em>, <em>lang:en</em>) for organization. No schema migration required.',
        sRagAgg_label: 'Aggregate Views',
        sRagAgg_summary: 'Attach a SQL view to a table so the assistant can answer count/sum/average questions with exact totals over the full table, not just the visible grid page — configured in the <strong>Settings tab</strong>.',
        sRagAgg_head: 'Aggregate Views',
        sRagAgg_desc: 'The grid page context sent to the assistant is limited to the current page (max 50 rows). Its first line states how much of the table it covers: when the page happens to hold every matching record the assistant is told so and may count and total those rows itself, but on a partial page it is instructed to never compute totals from it. To answer "how many" or "what\'s the total" questions correctly, write a SQL view with the exact aggregates you want (e.g. <code>public.v_companies_aggr</code>) directly in the database, then attach it to a table in <strong>RAG → Settings → Aggregate Views</strong>, always giving the fully qualified <code>schema.view</code> reference. The assistant queries the view read-only and gets back plain numbers — it never sees or writes SQL, and never chooses which columns to aggregate.',
        sRagAgg_li1: '<strong>Use full, descriptive column names</strong> — <code>total_companies</code>, <code>avg_contract_value_pln</code>, not <code>cnt</code> or <code>val</code>. The column name is the only label the model has for the number next to it.',
        sRagAgg_li2: '<strong>Spell out units or currency in the name</strong> when not obvious — <code>_pln</code>, <code>_days</code>, <code>_hours</code>.',
        sRagAgg_li3: '<strong>Give every aggregate a unique, specific name</strong> — avoid generic <code>sum1</code>/<code>sum2</code> when a view computes several sums.',
        sRagAgg_li4: '<strong>Use snake_case, English identifiers</strong>, consistent with the rest of the table schema.',
        sRagAgg_li5: '<strong>If the view groups rows</strong> (<code>GROUP BY</code> status, month, …), include the group value as its own column so multiple returned rows stay distinguishable in the answer.',
        sRagAgg_li6: '<strong>Keep the view read-only</strong> — it is queried unparameterized (<code>SELECT * FROM view LIMIT n</code>) on every matching question, so it must contain no side effects and no expensive joins that would slow every query down. The row cap <code>n</code> comes from <strong>Aggregate view rows</strong> in the global RAG settings (default 100); if the view returns more rows than that the block is cut off and the assistant will answer "not in the context" for the rows that were dropped.',
        sRagAgg_li7: '<strong>Always give the fully qualified reference</strong> — <code>schema.view</code> (e.g. <code>public.v_companies_aggr</code>, <code>spw_crm.v_deals_aggr</code>). There is no implicit default schema; a bare view name is rejected.',
        sRagAgg_li8: '<strong>A materialized view may be attached too</strong> — the picker lists it with a <em>materialized</em> hint. It answers faster, because the aggregate is precomputed, but it returns the figures from its last <code>REFRESH MATERIALIZED VIEW</code>; OpenSparrow never refreshes it. Attach one only if something else keeps it current, otherwise the assistant will state stale totals with full confidence. Only relations the application\'s database user may <code>SELECT</code> are offered.',
        sRagRollup_head: 'Subtotals (ROLLUPS)',
        sRagRollup_desc: 'A view grouped by two things at once (company <em>and</em> stage) holds the answer to "what is the total for stage X" only as several separate rows — and the assistant is forbidden to add figures up itself, so it would correctly but uselessly answer "not in the context". The server therefore rolls the view up before the question is sent: it groups the rows by each low-cardinality column on its own and appends the exact subtotals as a <code>ROLLUPS</code> block the model only has to quote. No configuration is needed; it applies to every attached view.',
        sRagRollup_li1: '<strong>Only additive columns are rolled up</strong> — a column is summed when every value is numeric and its name does not announce a non-additive statistic (<code>avg_</code>, <code>min_</code>, <code>max_</code>, <code>first_</code>, <code>last_</code>, <code>_pct</code>, <code>_ratio</code>, <code>id</code>). An average of averages is never computed; where the view has exactly one count column, an exact <code>derived_avg_…</code> is emitted instead.',
        sRagRollup_li2: '<strong>Coarse groupings win</strong> — the roll-up uses at most three grouping columns with 2-25 distinct values, coarsest first (a 5-value stage column before a 24-value company column). Columns whose values are longer than 80 characters (an aggregated contact or note blob) are never used as a grouping, and a column with a distinct value on almost every row is skipped as well.',
        sRagRollup_li3: '<strong>The block is size-capped</strong> — roughly 6 000 characters and 45 lines, applied per grouping so a list is never cut in half. A grouping that does not fit is dropped whole; its rows are still in the view above. Subtotals are also skipped when the view itself was truncated by <strong>Aggregate view rows</strong>, since they would then cover only part of the data.',
        sRagAgg_warn_label: 'Ownership restriction:',
        sRagAgg_warn: 'Only tables without row-level owner restriction can be paired with a view — a plain SQL view has no session or <code>user_id</code> to filter by. Attaching one to an owner-restricted table is rejected both in this UI and on the server.',
        sPrint_head: '16. Print Templates',
        sPrint_desc: '<strong>Data Management → Print</strong> — define printable/PDF-style document templates (quotes, invoices, labels) bound to a table or a view, rendered via <code>public/print.php</code>.',
        sPrint_source_label: 'Data source',
        sPrint_source: 'Bind a template to any table or PostgreSQL view; template fields are populated from the columns of the selected record.',
        sPrint_layout_label: 'Layout',
        sPrint_layout: 'Header, a repeating line-item section (for one-to-many subtable data, e.g. invoice lines), footer, and computed totals.',
        sPrint_pagination_label: 'Pagination',
        sPrint_pagination: 'Long line-item lists split automatically across pages using the <code>.pr-page</code> / <code>.pr-page-footer</code> print CSS classes.',
        sPrint_access_label: 'Printing a record',
        sPrint_access: 'From the record\'s grid row or detail view, choose a configured template to open <code>public/print.php?template=...&amp;id=...</code> in a new tab, ready for the browser\'s Print dialog or Save as PDF.',
        sPrint_config_label: 'Configuration',
        sPrint_config: 'Templates are stored in <code>spw_config.print</code>.',
        s9m_head: '9m. Data Anonymization (GDPR)',
        s9m_desc: '<strong>System → Anonymization</strong> — automates replacing PII column values with a configured replacement string. Truly anonymised data falls outside EU data protection law (GDPR). Config stored in <code>spw_config.anonymization</code>; run history in <code>spw_anonymization_log</code>. Each history entry can be expanded into a GDPR/EDPB compliance report (JSON, stored in <code>spw_anonymization_report</code>) with a Download button.',
        s9m_tab1_label: 'Rules tab',
        s9m_tab1: 'Define rules per column: select the table, a date/timestamp column and an age threshold (anonymize records older than N days), the PII column, and the replacement value (e.g. <code>***ANONYMIZED***</code>). The <em>Preview (dry run)</em> button counts how many rows each rule would anonymize without modifying any data — look before you run.',
        s9m_tab2_label: 'Schedule tab',
        s9m_tab2: 'Enable/disable the module and set frequency: <em>Manual only</em>, <em>Daily</em>, <em>Weekly</em>, or <em>Monthly</em>. The cron script (<code>cron/cron_anonymization.php</code>) enforces the window by checking the last successful run. <em>Run Now</em> triggers immediately, bypassing the frequency check. Cron Setup Guide provides ready-to-copy commands for Linux, Windows and Docker.',
        s9m_tab3_label: 'Suggestions tab',
        s9m_tab3: '<em>Scan Schema</em> cross-references your schema column names with the dictionary keywords and lists potential PII columns with matched keywords. Click <em>+ Add Rule</em> next to any match to create a rule directly.',
        s9m_tab4_label: 'Dictionary tab',
        s9m_tab4: 'Comma-separated PII keywords (e.g. <code>PESEL, NIP, email, phone, address, name</code>). Used by Suggestions for case-insensitive substring matching against column and display names. Log Cleanup section purges old <code>spw_anonymization_log</code> rows older than N days.',
        s9m_tab5_label: 'History tab',
        s9m_tab5: 'The last 50 executions from <code>spw_anonymization_log</code>, one row per run with its status and duration. Expand an entry to generate and download its GDPR/EDPB compliance report.',
        sLogo_head: '9n. Custom Logo',
        sLogo_desc: '<strong>Settings → Custom Logo</strong> — replace the default OpenSparrow logo shown in the frontend header with your own image.',
        sLogo_li1_label: 'Upload',
        sLogo_li1: 'PNG, JPEG or WEBP, up to 2 MB. The file is validated server-side (MIME sniffing, not just the extension) and stored under <code>public/assets/img/uploads/</code> with a random filename.',
        sLogo_li2_label: 'Storage',
        sLogo_li2: 'The active logo path is saved to <code>spw_config.settings</code> (<code>custom_logo_path</code>); the previous file is deleted automatically when a new logo is uploaded.',
        sLogo_li3_label: 'Remove',
        sLogo_li3: 'Click Remove logo to delete the file and settings key and revert to the default OpenSparrow logo.',
        sLogo_li4_label: 'Enable/disable',
        sLogo_li4: 'Independent "Show logo in header" checkbox — uncheck and Save to hide the logo entirely and return to the plain header (no logo), without deleting the uploaded file.',
        s1_t_clickstats: 'recorded UI clicks (user, time, element, and optionally the table and record in context). Only written while the Click Statistics module is enabled.',
        s9q_head: '9q. Click Statistics',
        s9q_desc: '<strong>System → Click Statistics</strong> records which page elements users actually click. Config in <code>spw_config.clickstats</code>; recorded clicks in <code>spw_clickstats</code>, written by <code>public/api/clickstats.php</code>. Requires the <code>3.3_clickstats</code> migration (Migrations → Initialize System Tables).',
        s9q_tab1_label: 'Settings tab',
        s9q_tab1: 'Two checkboxes. <em>Enable Click Statistics</em> switches collection on; <em>Record Table And Record</em> additionally stores which table and record was open, and unchecking it reduces every entry to user, time and element.',
        s9q_tab2_label: 'Log tab',
        s9q_tab2: 'The recorded clicks, newest first, 100 per page, filterable by element and by user, with a Top Elements rollup of the 20 most-clicked elements. Retention is manual: <em>Delete Older Than</em> trims the log to a rolling window of N days and <em>Clear Log</em> empties <code>spw_clickstats</code> entirely — there is no cron worker and no automatic expiry.',
        s9q_off_label: 'When disabled',
        s9q_off: 'Off means absent. With the module off no collector script is emitted into any page, so nothing is downloaded, no listener is installed and no request is ever sent. The endpoint independently re-checks the flag and answers 204 without writing, so a page left open when the module is switched off stops recording immediately.',
        s9q_privacy_label: 'What is collected',
        s9q_privacy: 'A click on a button, link or element carrying <code>data-stat</code> stores the element label (the <code>data-stat</code> value when present, otherwise a derived id/class/text label), the page script name, and the user id. Input values, query strings and form contents are never recorded. Clicks are buffered in the browser and delivered in batches via <code>navigator.sendBeacon</code>, so collection costs a handful of requests per session rather than one per click. A table name from a client is stored only if that user has access to it.',
        s9q_labels_label: 'Labels can contain record data',
        s9q_labels: 'When an element carries neither <code>data-stat</code> nor an id, the label falls back to the element\'s own visible text (up to 40 characters) — and in a list of records that text is record content: a file name on the Files page, a record title in a row link. Unchecking <em>Record Table And Record</em> does not change this; that switch only governs the separate table and record columns. Add <code>data-stat="…"</code> to an element to pin exactly what is stored for it.',
        s9l_head: '9l. Mass Edit &amp; Quick Data Cleanup',
        s9l_desc: 'Two bulk tools available to the <strong>editor</strong> role directly from the data grid. Both always show a preview before anything is written, and every applied change is recorded in <code>spw_users_log</code>.',
        s9l_edit_label: 'Mass Edit',
        s9l_edit: 'Select rows in the grid, pick one column and a new value, preview the affected rows, then apply. Backed by <code>api/mass_edit.php</code> (<code>mass_edit_preview</code> / <code>mass_edit_apply</code>); the table and column are validated against <code>spw_config.schema</code> and the update runs as a parameterized query.',
        s9l_dup_label: 'Mass Duplicate / Mass Delete',
        s9l_dup: 'The same endpoint also serves <code>mass_duplicate</code> (clone the selected records) and <code>mass_delete</code> (remove them), both scoped strictly to the explicitly selected record IDs.',
        s9l_clean_label: 'Quick Data Cleanup',
        s9l_clean: 'Find-and-replace across a single column of one table (<code>api/data_cleanup.php</code>). Options: case-insensitive, whole word only, and ignore accents. Preview reports the number of matching rows and sample values before <em>Apply</em> rewrites them.',
        s9l_role_label: 'Permissions',
        s9l_role: 'Both endpoints require the <code>editor</code> role and a valid CSRF token; viewers cannot reach them.',
        s10c_head: '10c. User Records',
        s10c_desc: '<strong>Data Management → User Records</strong> configures the "My records" panel that every user opens from the avatar menu — the list of records currently assigned to them across tables. Stored in <code>spw_config.user_records</code>.',
        s10c_map_label: 'Column Mapping tab',
        s10c_map: 'Per table, tick the columns that are concatenated into each assigned record\'s label. Leave a table unchecked to fall back to its first grid column automatically.',
        s10c_global_label: 'Global Settings tab',
        s10c_global: 'How many recently-assigned records are shown per table. Ownership itself comes from <code>spw_record_owners</code> (see section 9d).',
        s10d_head: '10d. Record Comments',
        s10d_desc: 'Threaded comments attached to any record, stored in <code>spw_comments</code> and served by <code>api/comments.php</code>. No admin tab is required — comments are available on every table in the schema.',
        s10d_where_label: 'Where they appear',
        s10d_where: 'As a Comments tab inside the edit form and as a counter badge in the data grid. The avatar menu also exposes "My comments" — everything the current user wrote, newest first, each entry linking back to its record.',
        s10d_perm_label: 'Rules',
        s10d_perm: 'Body text is capped at 4000 characters. Deleting a comment is a soft delete (timestamp), so the audit trail stays intact; viewers can read comments but not add them.',
        s10e_head: '10e. Private Notes',
        s10e_desc: 'A per-user notepad, stored in <code>spw_notes</code> and served by <code>api/notes.php</code>. Opened from the <strong>Notes</strong> entry in the avatar menu. Like comments, it needs no admin configuration.',
        s10e_private_label: 'Strictly private',
        s10e_private: 'Every query is scoped to the calling user — notes are never shared, and no role can read another account\'s notes. This is the difference from comments, which are attached to a record and visible to everyone who can open it.',
        s10e_link_label: 'Record link',
        s10e_link: 'A note may optionally reference a record (table + id), so it can be opened from the note straight back to what it is about. It can equally stand alone.',
        s10e_reminder_label: 'Reminders',
        s10e_reminder: 'A note can carry a <strong>reminder date</strong>. <code>cron/cron_notifications.php</code> picks up notes that are due or overdue and delivers them as in-app notifications under the bell icon, so a reminder set for a time earlier in the day still fires on that day\'s run. Body text is capped at 4000 characters and deletion is a soft delete, exactly as for comments.',
        sUpg_head: '17. Upgrading OpenSparrow',
        sUpg_desc: 'After pulling a new version, use <strong>System → Migrations</strong> to check for pending release migrations. The admin header displays a yellow upgrade notice when action is required.',
        sUpg_flow_head: 'Release workflow',
        sUpg_step1: 'Pull the new release: <code>git pull</code>',
        sUpg_step2: 'Open Admin → <strong>System → Migrations</strong>.',
        sUpg_step3: 'Run <strong>Apply Pending Migrations</strong> first (database schema changes).',
        sUpg_step4: 'Scroll to the <strong>Release Migrations</strong> section. Select the actions you want to apply and click <strong>Apply selected</strong>.',
        sUpg_step5: 'Verify the upgrade notice in the header has disappeared.',
        sUpg_what_head: 'What Release Migrations do',
        sUpg_what1: '<strong>Remove file</strong> — moves an obsolete file to <code>storage/migrations_backup/&lt;version&gt;/</code> then deletes it from the working tree.',
        sUpg_what2: '<strong>Remove config key</strong> — removes a deprecated field from a configuration key in the <code>spw_config</code> store; the previous value is written to <code>spw_config_log</code> first. Skipped if an admin saved that config while the migration ran.',
        sUpg_what3: '<strong>Deprecated (info only)</strong> — no action taken; the file is still present but generates a warning in <code>storage/logs/deprecations.log</code> if accessed.',
        sUpg_backup_head: 'Backups',
        sUpg_backup: 'Removed files are copied to <code>storage/migrations_backup/&lt;version&gt;/</code> before deletion. Config keys are DB-backed, so their pre-change value is kept in the <code>spw_config_log</code> audit trail instead. The applied history in the Migrations tab shows the backup location for each action.',
        sUpg_add_head: 'Adding a migration entry (for contributors)',
        sUpg_add: 'Every pull request that removes a file or a configuration key must add an entry to <code>config/migrations.json</code> in the same PR. In <code>removed_config_keys</code>, <code>file</code> names the <code>spw_config</code> key — the legacy <code>schema.json</code> spelling is still accepted and maps to key <code>schema</code>. Example:',
        s14_head: '14. Automations',
        s14_desc: 'Rule-based automation engine. Define triggers, conditions, and actions that execute server-side whenever a record is created, updated or deleted. Rules can be duplicated (creates a disabled copy) and enabled/disabled directly from the list by clicking the status badge.',
        s14_trigger_label: 'Triggers',
        s14_trigger: 'Three events: <strong>After create</strong> (INSERT), <strong>After update</strong> (PATCH) and <strong>After delete</strong> (DELETE). Delete rules see the row as it was immediately before deletion; the <strong>Update fields</strong> action is unavailable there because the row no longer exists.',
        s14_cond_label: 'Conditions (nested AND / OR)',
        s14_cond: 'Build multi-level condition trees. Each group has an AND / OR type toggle. Click <strong>+ Group</strong> to nest a sub-group. Supported operators: <code>equals</code>, <code>not equals</code>, <code>contains</code>, <code>not contains</code>, <code>is empty</code>, <code>is not empty</code>, comparisons <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code> (numeric first, then date, then text), plus change detection for update events: <code>changed</code>, <code>not changed</code>, <code>changed from</code>, <code>changed to</code> — they compare the value before and after the write. On create events the old value does not exist, so <code>changed</code> matches and <code>changed from</code> never does.',
        s14_actions_label: 'Action types',
        s14_actions: '<strong>Update fields</strong> — set any column on the triggering record. <strong>Send notification</strong> — insert an in-app notification for one or more users (select from a checklist; "Current user" = the user who triggered the event). <strong>Create record</strong> — insert a new row in any table in your schema. <strong>Send webhook</strong> — send a JSON payload (rule ID, event, table, record ID, mapped fields — or the full record when the mapping is empty, plus <code>old_data</code> on update and delete events) to an external http(s) endpoint using POST, PUT, PATCH or DELETE. This is the integration path for n8n, Make and custom receivers: point the URL at an n8n Webhook node. An optional secret adds an <code>X-Sparrow-Signature</code> header (HMAC SHA-256 of the body) so the receiver can verify the origin; it is stored encrypted and never shown back — leave the field blank to keep the saved value or press <strong>Clear secret</strong> to remove it. Custom <strong>headers</strong> carry the receiver\'s own auth (e.g. an n8n Header Auth credential). Header names stay visible but their values are stored encrypted and never sent back to the browser — leave a value blank to keep the saved one, remove the row to delete it; renaming a header clears its value. <code>Content-Type</code>, <code>User-Agent</code>, <code>Host</code> and <code>X-Sparrow-Signature</code> are reserved. <strong>On failure</strong> you can enable 1 or 2 retries with backoff — they apply only to timeouts and 5xx/429 responses, never to a 4xx, and because sending is synchronous each retry delays the user\'s save. <strong>Send email</strong> — queue an email (recipients, subject and message support templates) in <code>spw_automation_emails</code>; delivery runs via <code>cron/cron_notifications.php</code> and requires <code>AUTOMATION_EMAIL_FROM</code>. Webhook and email respect record ownership on <code>owner_restricted</code> tables and are written to the audit log. All values support template variables.',
        s14_vars_label: 'Template variables',
        s14_vars: '<code>{{ current_user.id }}</code> — ID of the user who triggered the event. <code>{{ record.FIELD }}</code> — value of any column from the triggering record. <code>{{ old_record.FIELD }}</code> — the column value before the update (empty on create events). <code>{{ today }}</code> — current date (YYYY-MM-DD); also works in condition values, e.g. deadline <code>&lt;=</code> <code>{{ today }}</code>.',
        s14_n8n_label: 'Connecting to n8n (or Make / any HTTP receiver)',
        s14_n8n_intro: 'The <strong>Send webhook</strong> action is the outbound integration path. OpenSparrow pushes record events to the receiver; no extra service or plugin is needed on either side.',
        s14_n8n1: 'In n8n, add a <strong>Webhook</strong> node, set the method to <code>POST</code> and copy its production URL.',
        s14_n8n2: 'In OpenSparrow, create a rule on the table you care about, pick the trigger event (<strong>After create</strong>, <strong>After update</strong> or <strong>After delete</strong>) and add any conditions.',
        s14_n8n3: 'Add a <strong>Send webhook</strong> action, paste the n8n URL and leave the payload mapping empty to receive the whole record — or map individual JSON keys to templates.',
        s14_n8n4: 'For authentication choose one: set a <strong>Secret</strong> and verify <code>X-Sparrow-Signature</code> in n8n (a Crypto node computing HMAC SHA-256 over the raw body, compared with an IF node), or add a <strong>header</strong> matching an n8n <em>Header Auth</em> credential. Both can be used together.',
        s14_n8n5: 'Save, edit a record, then open <strong>History</strong> — status <code>ok</code> means the receiver answered with a 2xx.',
        s14_n8n_payload: 'Body shape: <code>{ rule_id, event, table, record_id, triggered_by, data }</code>, plus <code>old_data</code> with the pre-change row on update and delete events — enough to diff without n8n keeping its own copy.',
        s14_n8n_warn_label: 'Loop risk:',
        s14_n8n_warn: 'if the n8n workflow writes back into the same table through the API, that write fires the rules again. Scope the rule with a condition (for example a status field the workflow does not touch) so it cannot re-trigger itself.',
        s14_history_label: 'Run History',
        s14_history: 'Click <strong>History</strong> on any rule to see the last 100 execution logs. Each row shows: time, table, record ID, event, status (<code>ok</code> / <code>error</code> / <code>skipped</code>), and error message. Status <code>skipped</code> means conditions were not met. Logs are stored in <code>spw_automation_runs</code>.',
        s14_note_label: 'Note',
        s14_note: 'Automations run synchronously on every matching write. Keep action chains short to avoid slowing down the API response — a webhook waits up to 5 s to connect and 10 s for a response, and each enabled retry adds another such attempt plus its backoff. Failing actions are logged but do not roll back the original record change.'
    },
    pl: {
        sidebarTitle: 'Spis treści',
        title: 'OpenSparrow - Dokumentacja Panelu Administracyjnego',
        subtitle: 'Konfiguruj aplikację frontendową, zarządzaj połączeniami z bazą danych oraz buduj dynamiczne dashboardy, kalendarze i przepływy pracy — bez pisania ani jednej linii kodu.',
        s0_head: '0. Pierwsze uruchomienie',
        s0_warn_strong: 'Świeża instalacja?',
        s0_warn_text: 'Panel administracyjny automatycznie wykrywa brak konfiguracji bazy danych (brak połączenia lub tabeli <code>spw_users</code>) i otwiera się bez wymagania logowania. Wykonaj następujące kroki:',
        s0_step1: 'Otwórz <code>/admin</code> w przeglądarce — panel uruchamia się w trybie konfiguracji z żółtym banerem.',
        s0_step2: 'Przejdź do <strong>System → Baza danych</strong>, wprowadź host, port, nazwę bazy, użytkownika i hasło, a następnie kliknij <strong>Zapisz konfigurację</strong>.',
        s0_step3: 'Przejdź do <strong>System → Migracje</strong> i kliknij <strong>Zastosuj oczekujące migracje</strong>. Spowoduje to utworzenie wszystkich tabel <code>spw_*</code> oraz dodanie domyślnego konta admina: login <code>admin</code>, hasło <code>admin</code>.',
        s0_step4: 'Przejdź do <code>/login</code>, zaloguj się jako <code>admin</code> / <code>admin</code>. Zostaniesz automatycznie przekierowany do <code>/admin</code>.',
        s0_step5: 'Przejdź do <strong>System → Użytkownicy</strong>, znajdź wiersz <em>admin</em> i kliknij <strong>Zmień hasło</strong>. Wprowadź aktualne hasło (<code>admin</code>) i ustaw silne nowe hasło.',
        s0_after: 'Od tego momentu panel administracyjny wymaga aktywnej sesji — możliwość obejścia logowania jest trwale zamknięta po utworzeniu tabeli <code>spw_users</code> w bazie danych.',
        s0b_head: '0b. Układ panelu administracyjnego',
        s0b_desc: 'Panel administracyjny korzysta ze zwijanego lewego paska bocznego z pięcioma sekcjami (Przegląd, Zarządzanie danymi, Przepływy, Baza wiedzy, System).',
        s0b_data_label: 'Zarządzanie danymi',
        s0b_data: 'Tablica, Kalendarz, Import CSV, Dashboard, ETL, Pliki, Wydruki, Schemat, Rekordy użytkownika, Widoki. Dodaj tabelę, Podgląd menu, Mapa schematu i Kreator M2M to zakładki wewnętrzne Schematu, a nie osobne pozycje paska bocznego.',
        s0b_workflows_label: 'Przepływy pracy',
        s0b_workflows: 'Automatyzacje, Menedżer przepływów.',
        s0b_kb_label: 'Baza wiedzy',
        s0b_kb: 'Dokumenty RAG.',
        s0b_system_label: 'System',
        s0b_system: 'Anonimizacja, Kopie zapasowe, Cron powiadomień, Systemy demo, Diagnostyka, Migracje, Wydajność, Ustawienia, Użytkownicy. Audyt i migawki to zakładka wewnętrzna Ustawień.',
        s0b_save_label: 'Zapisz konfigurację',
        s0b_save: 'Zapisuje aktualnie edytowaną konfigurację do magazynu <code>spw_config</code> w bazie danych — poza <code>database</code> i <code>security</code>, które pozostają plikami na dysku. Po zapisie pojawia się zielona pigułka potwierdzająca, która konfiguracja została zapisana.',
        s0b_guard_label: 'Ochrona przed niezapisanymi zmianami',
        s0b_guard: 'Wyświetla monit przed odrzuceniem zmian w zakładkach konfiguracyjnych.',
        s0b_debug_label: 'Tryb Debug FE',
        s0b_debug: 'Przełącznik w nagłówku. Po włączeniu udostępnia panel <code>#debug</code> z nieprzetworzonymi danymi odpowiedzi API.',
        s0b_docs_label: 'Ikona dokumentacji (książka)',
        s0b_docs: 'Otwiera tę stronę dokumentacji.',
        s1_head: '1. Wymagania techniczne i struktura bazy danych',
        s1_desc: 'Przed konfiguracją OpenSparrow upewnij się, że baza PostgreSQL spełnia następujące wymagania:',
        s1_pk_label: 'Klucze główne (obowiązkowe)',
        s1_pk: 'Każda tabela <strong>musi</strong> mieć kolumnę klucza głównego o nazwie <code>id</code> (zazwyczaj <code>SERIAL</code> lub <code>BIGSERIAL</code>). OpenSparrow opiera się na tej dokładnej nazwie do edycji, usuwania i wyświetlania rekordów.',
        s1_fk_label: 'Klucze obce (relacje)',
        s1_fk: 'Używaj standardowych kluczy obcych PostgreSQL do łączenia tabel. Interfejs wykrywa je automatycznie. Zalecana konwencja nazewnictwa: <code>nazwa_tabeli_id</code>.',
        s1_enum_label: 'Typy ENUM',
        s1_enum: 'Niestandardowe typy ENUM PostgreSQL są w pełni obsługiwane i renderowane jako menu <code>&lt;select&gt;</code>.',
        s1_bool_label: 'Typy Boolean',
        s1_bool: 'Kolumny logiczne renderowane są jako przełączniki w formularzach edycji i jako filtry rozwijane w siatkach danych.',
        s1_schema_label: 'Schemat systemowy',
        s1_schema: 'OpenSparrow przechowuje swoje wewnętrzne tabele (<code>spw_*</code>) w dedykowanym schemacie PostgreSQL (domyślnie: <code>app</code>).',
        s1_systables_head: 'Tabele systemowe (prefiks <code>spw_*</code>)',
        s1_systables_desc: 'OpenSparrow rezerwuje zestaw wewnętrznych tabel, wszystkie z prefiksem <code>spw_</code>, tworzonych w skonfigurowanym schemacie systemowym:',
        s1_t_users: 'konta użytkowników frontendowych (id, nazwa, hash hasła, rola, flaga aktywności, opcjonalne dane kontaktowe).',
        s1_t_users_log: 'ślad audytu akcji użytkowników (LOGIN, LOGOUT, operacje CRUD).',
        s1_t_notifications: 'powiadomienia w aplikacji dla każdego użytkownika, generowane przez cron.',
        s1_t_notifications_log: 'dziennik wykonania crona powiadomień: czas start/koniec, status, źródło wyzwolenia, liczba przetworzonych źródeł i utworzonych powiadomień.',
        s1_t_files: 'metadane plików przesłanych przez moduł Pliki.',
        s1_t_login_attempts: 'dziennik prób logowania używany przez ogranicznik częstotliwości.',
        s1_t_comments: 'komentarze użytkowników dołączone do dowolnego rekordu.',
        s1_t_snapshots: 'migawki JSONB rekordów przechwytywane po każdym INSERT lub UPDATE.',
        s1_t_owners: 'dziennik własności rekordów (append-only). Bieżący właściciel to wiersz z <code>is_current = true</code>.',
        s1_t_migrations: 'śledzenie migracji. Jeden wiersz na zastosowaną migrację.',
        s1_t_config: 'magazyn konfiguracji aplikacji — jeden wiersz JSONB na klucz (schema, menu, settings, dashboard, calendar, board, workflows, automations, views, files, print, anonymization, user_records, user_policy, user_table_access, rag, etl, etl_flows, clickstats), wersjonowany blokadą optymistyczną.',
        s1_t_config_log: 'pełna historia zmian konfiguracji: poprzednia wartość klucza, kto i kiedy ją zmienił.',
        s1_t_notes: 'prywatne notatki użytkownika, opcjonalnie powiązane z rekordem i z datą przypomnienia dostarczanego przez cron powiadomień.',
        s1_t_rag: 'baza wiedzy: dokumenty (<code>spw_rag_files</code>), ich fragmenty pełnotekstowe (<code>spw_rag_chunks</code>) oraz historia zapytań (<code>spw_rag_queries</code>, <code>spw_rag_query_sources</code>).',
        s1_t_automations: 'historia wykonań reguł automatyzacji (<code>spw_automation_runs</code>) i kolejka wychodzących e-maili (<code>spw_automation_emails</code>).',
        s1_t_imports: 'przebiegi importu CSV (<code>spw_imports</code>) wraz ze szczegółami wierszy w <code>spw_import_rows_log</code>.',
        s1_t_etl: 'historia wykonań zadań ETL (<code>spw_etl_log</code>) oraz przebiegi przepływów i ich kroków (<code>spw_etl_flow_run_log</code>, <code>spw_etl_flow_step_log</code>).',
        s1_t_anon: 'historia anonimizacji (<code>spw_anonymization_log</code>) i wygenerowane raporty zgodności RODO/EROD (<code>spw_anonymization_report</code>).',
        s1_t_release_migrations: 'zastosowane migracje wydań z <code>config/migrations.json</code> — jeden wiersz na wersję.',
        s1_note_strong: 'Uwaga:',
        s1_note_text: 'Tabele zaczynające się od <code>spw_</code> są traktowane jako tabele systemowe i są <strong>pomijane</strong> na liście <em>Synchronizuj tabele z bazy danych</em>.',
        s2_head: '2. Konfiguracja schematu i siatki',
        s2_desc: 'Zakładka <strong>Schemat</strong> to rdzeń konfiguracji — mapuje tabele bazy danych na siatki i formularze frontendowe.',
        s2_addtable_head: 'Dodaj tabelę (Zarządzanie danymi → Dodaj tabelę)',
        s2_addtable_desc: 'Dedykowana zakładka <strong>Dodaj tabelę</strong> tworzy nową fizyczną tabelę PostgreSQL i opcjonalnie rejestruje ją w <code>spw_config.schema</code>.',
        s2_name_label: 'Nazwa tabeli i schemat bazy danych',
        s2_name: 'Tylko małe litery. Kolumna <code>id serial PRIMARY KEY</code> jest zawsze dodawana automatycznie.',
        s2_display_label: 'Nazwa wyświetlana',
        s2_display: 'Automatycznie wypełniana z nazwy tabeli. Używana jako etykieta w menu i nagłówkach po zarejestrowaniu w <code>spw_config.schema</code>.',
        s2_presets_label: 'Presety kolumn',
        s2_presets: 'Zaznacz <em>Timestamps</em>, aby automatycznie dodać kolumny <code>created_at</code> i <code>updated_at</code>.',
        s2_columns_label: 'Opcje kolumny',
        s2_columns: 'Nazwa i typ, Not Null, Wartość domyślna, Indeks (<code>btree</code>, <code>hash</code>, <code>unique</code>), Komentarz, Klucz obcy.',
        s2_register_label: 'Zarejestruj w schemacie aplikacji',
        s2_register: '(domyślnie zaznaczone): Wpis tabeli jest automatycznie zapisywany do <code>spw_config.schema</code>.',
        s2_addcol_label: 'Dodaj nowe kolumny (zakładka Schemat)',
        s2_addcol: 'Kliknij <em>+ Dodaj kolumnę</em> wewnątrz konfiguracji istniejącej tabeli.',
        s2_synctables_label: 'Synchronizuj tabele z bazy danych',
        s2_synctables: 'Pobiera wszystkie tabele z podłączonej bazy i scala je z konfiguracją schematu. Tabele z prefiksem <code>spw_</code> są pomijane.',
        s2_synccols_label: 'Synchronizuj kolumny z bazy danych',
        s2_synccols: 'Pobiera kolumny dla danej tabeli i dodaje brakujące. Odczytuje też komentarze <code>COMMENT ON COLUMN</code>.',
        s2_coldesc_label: 'Opis kolumny (tooltip)',
        s2_coldesc: 'Pojawia się jako podpowiedź przy nagłówkach kolumn w siatce danych.',
        s2_preview_label: 'Podgląd paska bocznego na żywo',
        s2_preview: 'Aktualizuje się w czasie rzeczywistym podczas edycji Nazwy wyświetlanej, Ikony lub flagi Ukryj.',
        s2_typemap_label: 'Inteligentne mapowanie typów',
        s2_typemap: 'Typy PostgreSQL są automatycznie mapowane na typy frontendowe (Tekst, Liczba, Data, Boolean, Enum).',
        s2_remove_label: 'Usuń tabele',
        s2_remove: 'Usuwa tabelę tylko z konfiguracji JSON — nie usuwa fizycznej tabeli z PostgreSQL.',
        s2_fksearch_label: 'Wyszukiwanie i wyświetlanie klucza obcego',
        s2_fksearch: 'Przypisz wiele kolumn wyświetlanych do klucza obcego. Frontend renderuje je jako pola wyszukiwania.',
        s2_visibility_label: 'Widoczność i kolejność',
        s2_visibility: 'Przełącz widoczność kolumn w siatce i zmień kolejność strzałkami W górę/W dół.',
        s2_validation_label: 'Reguły walidacji (regex)',
        s2_validation: 'Wymuszaj ścisłe formaty za pomocą wyrażeń regularnych.',
        regex_phone: 'Telefon (9-15 cyfr, opcjonalny +)',
        regex_postal: 'Kod pocztowy (XX-XXX)',
        regex_username: 'Nazwa użytkownika (3-16 znaków)',
        regex_price: 'Cena / dziesiętna',
        regex_date: 'Data (RRRR-MM-DD)',
        regex_password: 'Silne hasło',
        s2_subtables_head: 'Podtabele (relacje jeden-do-wielu)',
        s2_subtables_desc: '<strong>+ Dodaj podtabelę</strong> wyświetla powiązane rekordy podrzędne wewnątrz widoku szczegółowego rekordu nadrzędnego.',
        s2_sub_open: 'Otwórz edytor schematu tabeli nadrzędnej i kliknij <em>+ Dodaj podtabelę</em>.',
        s2_sub_target_label: 'Tabela docelowa',
        s2_sub_target: 'tabela podrzędna do wyświetlenia.',
        s2_sub_fkcol_label: 'Kolumna klucza obcego',
        s2_sub_fkcol: 'kolumna w tabeli podrzędnej odwołująca się do <code>id</code> tabeli nadrzędnej.',
        s2_img_head: 'Zdjęcia rekordu',
        s2_img_desc: 'Galerie zdjęć per tabela, włączane w zakładce Schema. Każdy rekord może mieć wiele zdjęć wgrywanych z formularza edycji i wyświetlanych jako miniatury w gridzie.',
        s2_img_enable_label: 'Włącz',
        s2_img_enable: 'włącza obsługę zdjęć dla tabeli; konfiguracja jest zapisywana razem ze schematem tabeli (<code>tables[table].images</code>).',
        s2_img_max_label: 'Maks. na rekord',
        s2_img_max: 'limit od 1 do 50 zdjęć na rekord.',
        s2_img_grid_label: 'Pokaż w gridzie',
        s2_img_grid: 'gdy włączone, grid pokazuje kolumnę miniatur (do 4 podglądów) z podglądem po najechaniu; kliknięcie otwiera pełne zdjęcie w nowej karcie.',
        s2_img_upload_label: 'Wgrywanie',
        s2_img_upload: 'zdjęcia wgrywa się i usuwa w zakładce Images formularza edycji rekordu (niedostępne przy tworzeniu — najpierw zapisz rekord); krok przepływu, którego tabela ma galerię, również udostępnia pole wgrywania — zdjęcie zostaje dołączone po zapisaniu rekordu tego kroku. Używają tego samego magazynu i limitów rozmiaru/rozszerzeń co moduł Plików.',
        s3_head: '3. Kreator dashboardu',
        s3_desc: 'Zakładka <strong>Dashboard</strong> tworzy widoki analityczne. Układ używa stałej <strong>siatki 3-kolumnowej</strong>; każdy widget zajmuje 1, 2 lub 3 kolumny.',
        s3_types_head: 'Typy widgetów',
        s3_stat_label: 'Kafelek statystyk (Stat Card)',
        s3_stat: 'Kolorowy kafelek z pojedynczą wartością agregatową (COUNT, SUM lub AVG) wybranej kolumny.',
        s3_bar_label: 'Wykres słupkowy (poziomy / pionowy)',
        s3_bar: 'Grupuje wiersze według wybranej kolumny. Każdy słupek jest klikalny.',
        s3_pie_label: 'Wykres kołowy',
        s3_pie: 'Ten sam model grupowania/agregacji. Każdy wycinek jest klikalny.',
        s3_line_label: 'Wykres liniowy (szereg czasowy)',
        s3_line: 'Pokazuje wartość w czasie zamiast grupowania po kategorii. Konfigurujesz kolumnę osi czasu (X), ziarnistość (Dzień / Tydzień / Miesiąc / Rok), kolumnę agregacji (Y) i jej funkcję (Count / Sum / Average). Opcjonalny checkbox <em>Fill area under line</em> wypełnia obszar pod linią.',
        s3_list_label: 'Lista danych',
        s3_list: 'Najlepsze N wierszy posortowane według wybranej kolumny.',
        s3_props_head: 'Rozmiary widgetów',
        s3_width_label: 'Szerokość',
        s3_height_label: 'Wysokość',
        s3_height: 'Mała (140 px), Średnia (280 px), Duża (440 px).',
        s3_mobile: 'Na urządzeniach mobilnych wszystkie widgety zwijają się do jednej kolumny.',
        s3_period_head: 'Globalny filtr okresu i trendy',
        s3_period_desc: 'Nagłówek dashboardu udostępnia wybór okresu (Cały okres / Dzisiaj / Ostatnie 7 dni / Ostatnie 30 dni / Bieżący miesiąc). Filtruje wszystkie widgety po pierwszej kolumnie datowej; karty count i sum pokazują dodatkowo zmianę procentową względem poprzedniego okresu o tej samej długości.',
        s3_export_desc: 'Użytkownicy z uprawnieniem eksportu widzą na każdym widgecie (po najechaniu) przycisk CSV pobierający aktualnie wyświetlane dane — bez zapytania do serwera, z uwzględnieniem aktywnego filtra okresu.',
        s3_filter_head: 'Warunki filtrowania (WHERE)',
        s3_filter_desc: 'Każdy widget obsługuje warunki filtrowania łączone operatorami AND/OR.',
        s3_filter_ops: 'Dostępne operatory',
        s3_preview_head: 'Podgląd na żywo',
        s3_preview_desc: 'Panel <strong>Podgląd na żywo</strong> renderuje rzeczywisty widget z przykładowymi danymi. Używa stałych danych testowych — nie wymaga połączenia z bazą danych.',
        s3_global_head: 'Ustawienia globalne',
        s3_global_desc: 'Kontroluje pozycję dashboardu w menu i odstęp między widgetami. Domyślnie <code>20px</code>.',
        s4_head: '4. Moduł kalendarza',
        s4_desc: 'Zakładka <strong>Kalendarz</strong> powiązuje kolumny dat z bazy danych z wizualnym kalendarzem.',
        s4_sources_label: 'Źródła danych',
        s4_sources: 'Nakładaj wiele tabel na jeden kalendarz.',
        s4_color_label: 'Kodowanie kolorami',
        s4_color: 'Przypisz kolor każdemu źródłu.',
        s4_context_label: 'Kontekst wiersza',
        s4_context: 'Pełny wiersz bazy danych jest dołączony do każdego zdarzenia kalendarza.',
        s4b_head: '4b. Moduł tablicy (Kanban)',
        s4b_desc: 'Zakładka <strong>Board</strong> zamienia tabelę w tablicę Kanban: rekordy stają się kartami ułożonymi w torach, po jednym torze na wartość kolumny statusu. Przeciągnięcie karty do innego toru natychmiast zmienia tę kolumnę.',
        s4b_multi_label: 'Wiele tablic',
        s4b_multi: 'Zakładka przechowuje nazwaną listę tablic (<code>spw_config.board</code>, tablica <code>boards</code>), a nie pojedynczą. Każda tablica jest konfigurowana niezależnie i dostaje własny wpis w menu frontendu, więc jedna instalacja może prowadzić kilka tablic na różnych tabelach.',
        s4b_table_label: 'Tabela źródłowa',
        s4b_table: 'Dla każdej tablicy wybierz jedną tabelę, której rekordy będą pokazane jako karty.',
        s4b_status_label: 'Kolumna statusu',
        s4b_status: 'Najważniejsze ustawienie — jej wartości stają się torami tablicy. Zalecana kolumna typu <code>enum</code>, aby tory i ich kolory pochodziły z <code>enum_colors</code>; dla kolumn nie-enum tory są wyprowadzane z istniejących wartości.',
        s4b_cards_label: 'Zawartość karty',
        s4b_cards: 'Wybierz kolumnę tytułu oraz opcjonalne pola szczegółowe pokazywane na każdej karcie; klucze obce wyświetlane są jako etykieta.',
        s4b_dnd_label: 'Przeciąganie',
        s4b_dnd: 'Dostępne dla roli editor. Zmiany są walidowane względem dozwolonych wartości, respektują własność rekordu i są zapisywane w logu audytu (<code>BOARD_MOVE</code>).',
        s5_head: '5. Kreator przepływów pracy',
        s5_desc: 'Zakładka <strong>Przepływy pracy</strong> tworzy wieloetapowe kreatory prowadzące użytkowników przez ustrukturyzowane wprowadzanie danych.',
        s5_steps_label: 'Konfiguracja kroków',
        s5_steps: 'Dodawaj kolejne kroki i wybierz docelową tabelę dla każdego kroku.',
        s5_link_label: 'Łączenie relacyjne',
        s5_link: 'Powiąż rekordy podrzędne z nadrzędnymi przez wybór kolumny klucza obcego.',
        s5_multi_label: 'Wiele rekordów',
        s5_multi: 'Włącz <em>Zezwól na dodawanie wielu rekordów</em> dla wybranego kroku.',
        s5_validation_head: 'Walidacja kroków',
        s5_validation_desc: 'Przepływ pracy nie może być zapisany, jeśli którykolwiek krok nie ma <strong>nazwy</strong> ani <strong>tabeli docelowej</strong>, lub gdy przepływ nie ma żadnych kroków. Niekompletne kroki są wyróżnione czerwoną lewą krawędzią i oznaczeniem "— incomplete". Przycisk Zapisz jest blokowany do czasu uzupełnienia wszystkich kroków.',
        s5_proc_head: 'Procedura składowana przy przejściu dalej',
        s5_proc_desc: 'Każdy krok może wywołać <strong>procedurę</strong> PostgreSQL w momencie kliknięcia <em>Następny krok</em>. Schemat i nazwę procedury wybiera się z listy rozwijanej zawierającej procedury składowane z bazy; następnie pojawia się jeden wiersz na każdy zadeklarowany parametr <code>IN</code>, zasilany albo z pola przepływu (wybierasz krok źródłowy i kolumnę — krok bieżący lub dowolny wcześniejszy), albo ze stałej wartości wpisanej ręcznie. Procedura uruchamia się <strong>przed</strong> przejściem dalej i otrzymuje wyłącznie wartości z formularza, nigdy identyfikatorów rekordów — nic nie jest zapisywane do bazy aż do ekranu podsumowania. Jeśli procedura zgłosi wyjątek, jego komunikat trafia do użytkownika, a kreator zostaje na bieżącym kroku, dzięki czemu procedura może działać jako walidacja po stronie serwera. Konfiguracja jest zapisywana przy kroku w <code>steps[].procedure</code>.',
        s6_head: '6. Zarządzanie użytkownikami',
        s6_desc: 'Zakładka <strong>Użytkownicy</strong> (<em>System → Użytkownicy</em>) zarządza wszystkimi kontami.',
        s6_roles_label: 'Role',
        s6_admin: 'dostęp tylko do panelu administracyjnego. Nie może logować się do aplikacji frontendowej.',
        s6_editor: 'pełny dostęp CRUD do aplikacji frontendowej. Nie ma dostępu do panelu administracyjnego.',
        s6_viewer: 'dostęp tylko do odczytu frontendu. Nie może modyfikować rekordów ani uzyskać dostępu do panelu administracyjnego.',
        s6_pwd_label: 'Zmiana hasła',
        s6_pwd: 'Własne konto wymaga podania aktualnego hasła. Inne konta — bez aktualnego hasła (nadpisanie przez admina).',
        s6_status_label: 'Status aktywności',
        s6_status: 'Przełącz Aktywny / Nieaktywny, aby odwołać lub przywrócić dostęp do logowania.',
        s7_head: '7. Konfiguracja bazy danych',
        s7_desc: 'Zarządzaj połączeniem PostgreSQL z <strong>System → Baza danych</strong>.',
        s7_schema_label: 'Schemat systemowy',
        s7_schema: 'Ustawia schemat PostgreSQL dla wszystkich tabel <code>spw_*</code>. Domyślnie <code>app</code>.',
        s7_test_label: 'Testuj zapisane połączenie',
        s7_test: 'Najpierw kliknij <em>Zapisz konfigurację</em> — test odczytuje zapisany <code>database.json</code>, nie wartości z formularza.',
        s7_login_label: 'Ochrona logowania',
        s7_login: 'Ogranicznik częstotliwości (IP + nazwa użytkownika), tokeny CSRF, odcisk palca sesji, 8-godzinny czas życia sesji, ciasteczka <code>SameSite=Lax</code> / <code>HttpOnly</code>.',
        s8_head: '8. Kopie zapasowe tabel',
        s8_desc: '<strong>System → Kopie zapasowe tabel</strong> tworzy kopie wybranych tabel bezpośrednio w PostgreSQL.',
        s8_format_label: 'Format nazwy kopii',
        s8_what_label: 'Co jest kopiowane',
        s8_what: 'Struktura kolumn i wszystkie wiersze. Indeksy i ograniczenia <strong>nie</strong> są kopiowane.',
        s9_head: '9. Diagnostyka systemu, Cron i konfiguracja',
        s9_migrations_label: 'Migracje bazy danych',
        s9_migrations: '<em>System → Migracje</em>. Kliknij <strong>Zastosuj oczekujące migracje</strong>.',
        s9_diagnostics_label: 'Diagnostyka systemu',
        s9_diagnostics: 'Bieżące sprawdzenie wersji PHP, rozszerzeń, uprawnień do zapisu i łączności z bazą danych.',
        s9_cron_label: 'Uruchom Cron powiadomień',
        s9_cron: 'Wykonuje <code>cron/cron_notifications.php</code> natychmiastowo. Każde uruchomienie jest rejestrowane w <code>spw_users_notifications_log</code>.',
        s9b_head: '9b. Audyt i migawki rekordów',
        s9b_desc: '<strong>Ustawienia → Audyt i migawki</strong> (zakładka wewnętrzna Ustawień, nie osobna pozycja paska bocznego) steruje modułem migawek rekordów — przechwytuje pełny stan rekordu po każdej operacji zapisu.',
        s9b_how_label: 'Jak działa',
        s9b_how: 'Każdy INSERT i UPDATE zapisuje kopię JSONB do <code>spw_record_snapshots</code>, powiązaną z <code>spw_users_log</code> przez <code>log_id</code>.',
        s9b_toggle_label: 'Przełącznik',
        s9b_toggle: 'Zapisuje do <code>spw_config.settings</code>. Wchodzi w życie przy następnym żądaniu. Nadpisywany przez zmienną środowiskową <code>RECORD_SNAPSHOTS_ENABLED</code>.',
        s9b_storage_label: 'Wzrost przestrzeni dyskowej',
        s9b_storage: 'Monitoruj rozmiar tabeli i wdrożyj politykę przechowywania dla instalacji o dużym wolumenie.',
        s9c_head: '9c. Migracje bazy danych',
        s9c_desc: '<strong>System → Migracje</strong> — jedyne miejsce do stosowania i śledzenia wszystkich zmian schematu tabel <code>spw_*</code>.',
        s9c_applied: 'Zastosowane migracje są rejestrowane w <code>spw_migrations</code>. Ponowne uruchomienie jest zawsze bezpieczne.',
        s9c_adding_label: 'Dodawanie migracji (deweloperzy)',
        s9c_adding: 'Dodaj nowy wpis do tablicy <code>$migrations</code> w <code>includes/admin/migrations.php</code> (akcja <code>init_db</code>) i powiel klucz na liście <code>$known</code> w tym samym pliku oraz w <code>$knownMig</code> w <code>includes/admin/overview.php</code>. Nigdy nie modyfikuj istniejących wpisów.',
        s9d_head: '9d. Własność rekordów',
        s9d_desc: 'Każdy rekord może mieć <strong>właściciela</strong> śledzonego w <code>spw_record_owners</code>.',
        s9d_auto_label: 'Automatyczne przypisanie',
        s9d_auto: 'Twórca rekordu jest automatycznie ustawiany jako właściciel przy INSERT.',
        s9d_change_label: 'Zmiana właściciela',
        s9d_change: 'Rola Edytora/Admina: wybierz użytkownika i kliknij <em>Zmień właściciela</em> w zakładce Historia rekordu.',
        s9d_history_label: 'Pełna historia',
        s9d_history: 'Każda zmiana dopisuje wiersz. Żadne dane nie są usuwane.',
        s9o_head: '9o. Dostęp do frontendu per użytkownik',
        s9o_desc: 'Zakładka <strong>Użytkownicy → Access</strong> ogranicza użytkownika frontendu do podzbioru tabel ze schematu, skonfigurowanych widoków, szablonów wydruku, tablic i procesów (workflow). Grupy są niezależne, a całość działa niezależnie od roli: Viewer ograniczony do dwóch tabel nadal widzi je tylko do odczytu.',
        s9o_where_label: 'Gdzie jest przechowywane',
        s9o_where: 'Klucz <code>user_table_access</code> w <code>spw_config</code>, w postaci <code>{"users": {"&lt;id&gt;": {"tables": [], "views": [], "prints": [], "boards": [], "workflows": []}}}</code>. Widoki i wydruki nadaje się po nazwie, tablice i procesy po ich id — żadna z tych konfiguracji nie wiąże się z jedną tabelą tak, jak strona siatki.',
        s9o_empty_label: 'Pusty wybór',
        s9o_empty: 'W każdej grupie oznacza <strong>brak ograniczeń</strong> dla tej grupy, a nie „brak dostępu" — każde istniejące konto zachowuje pełny dostęp, dopóki nie zaznaczysz mu pozycji. Aby odciąć użytkownika całkowicie, dezaktywuj konto.',
        s9o_admin_label: 'Konta administratorów',
        s9o_admin: 'Nigdy nie są ograniczane i nie pojawiają się na liście — pracują w tym panelu i muszą widzieć cały schemat.',
        s9o_scope_label: 'Co obejmuje',
        s9o_scope: 'Menu, siatkę, strony tworzenia/edycji, edycję masową, czyszczenie danych, pliki, kontekst RAG, widżety dashboardu, źródła kalendarza, tablice oraz strony Widoków i Wydruków. Zakładki podtabel w <code>edit.php</code> również są filtrowane, bo zakładka pokazuje pełne wiersze tabeli podrzędnej. Egzekwowanie jest po stronie serwera na każdym endpoincie; ukrywanie pozycji menu jest wyłącznie kosmetyczne.',
        s9o_hidden_label: 'Ukryte tabele pomocnicze',
        s9o_hidden: 'Tabele oznaczone jako <em>ukryte</em> nie mają pozycji w menu ani własnej siatki, więc nie pojawiają się na liście w zakładce. Są nadawane automatycznie razem z tabelą, której są podtabelą, przechodnio — inaczej ograniczenie użytkownika po cichu zabrałoby zakładki podtabel, których nikt nie mógłby przywrócić. Grupa Tables wypisuje je pod listą w miarę zaznaczania rodziców. Podtabele widoczne nigdy nie są nadawane automatycznie: te pozostają Twoim jawnym wyborem.',
        s9p_head: '9p. Dane kontaktowe użytkownika',
        s9p_desc: 'Każde konto może mieć imię, nazwisko, adres e-mail i numer telefonu. Dane są wyłącznie informacyjne — nie służą do logowania, powiadomień ani nie są pokazywane na frontendzie.',
        s9p_where_label: 'Gdzie',
        s9p_where: 'Ustawiasz je w <strong>Użytkownicy → Manage Users</strong>: w formularzu „Add New User" albo przyciskiem „Edit Details" w wierszu. Widoczne w kolumnach Name / Email / Phone.',
        s9p_optional_label: 'Opcjonalne',
        s9p_optional: 'Wszystkie cztery pola mogą pozostać puste (pusta komórka pokazuje myślnik). E-mail jest sprawdzany tylko pod kątem poprawnego formatu i nie musi być unikalny.',
        s9p_scope_label: 'Zakres',
        s9p_scope: 'Tylko panel administracyjny. Żadna część frontendu nie czyta tych kolumn i nie są one udostępniane przez publiczne API.',
        s9o_limits_label: 'Znane ograniczenia',
        s9o_bindings_label: 'Tablice i procesy nadal potrzebują swoich tabel',
        s9o_bindings: 'Nadanie tablicy lub procesu nie nadaje tabel, z których korzystają — te dwie rzeczy zaznacza się osobno i obie muszą być spełnione. Tablica, której tabeli nie nadałeś, nie pojawi się na liście, a proces zostaje pominięty, gdy którykolwiek jego krok celuje w tabelę niedostępną dla użytkownika. Nadając tablicę lub proces, nadaj więc również ich tabele, inaczej pozycja po prostu się nie pokaże. Tak ma być: tablica i proces to sposób patrzenia na dane, nigdy obejście tego, kto może je zobaczyć.',
        s9o_views_label: 'Widoki i wydruki czytają ponad granicą',
        s9o_views: 'Widok lub wydruk, do którego użytkownik ma dostęp, może czytać z tabel, do których dostępu nie ma. Zapytanie jest zdefiniowane w konfiguracji i nie wynika z nadanych tabel, więc nadanie widoku nadaje wszystko, co ten widok czyta — dostęp do tabel nie filtruje jego wnętrza. Traktuj te dwie decyzje osobno: zanim nadasz widok lub szablon wydruku, sprawdź, co on faktycznie wybiera. Jeśli miesza dane przeznaczone dla różnych odbiorców, rozdziel go na widoki per odbiorca, zamiast liczyć na to, że dostęp do tabel zasłoni część danych.',
        s9o_limits: 'Etykiety kluczy obcych nadal rozwijają się ponad granicą — tabela dozwolona może pokazać nazwę z ograniczonej. Na tym polega granica: klucz obcy ujawnia nazwę, a podtabela wiersze, dlatego podtabele są filtrowane, a etykiety FK nie.',
        s9e_head: '9e. Domyślne sortowanie i limit wczytywania siatki',
        s9e_sort_label: 'Domyślna kolejność sortowania',
        s9e_sort: 'Jedna lub więcej reguł sortowania (kolumna + ASC/DESC). Awaryjnie stosowane jest <code>id DESC</code>.',
        s9e_limit_label: 'Początkowy limit wczytywania',
        s9e_limit: 'Maksymalna liczba wierszy przy pierwszym wczytaniu. <code>0</code> oznacza brak limitu.',
        s9e_stored_label: 'Przechowywane w',
        s9e_stored: 'jako <code>"default_sort"</code> i <code>"initial_limit"</code>.',
        s9f_head: '9f. Szybkie dodawanie w podtabelach',
        s9f_desc: 'Nagłówki bloków podtabel pokazują przycisk <strong>+</strong>, który przekierowuje do <code>create.php</code> z wstępnie wypełnionym kluczem obcym. Widoczny tylko dla ról Edytor i Admin.',
        s9f2_head: '9f2. Przyciski akcji w siatce',
        s9f2_desc: 'Ostatnia kolumna siatki zawiera przycisk rozwijany <code>more_vert.png</code>; kliknięcie otwiera mały panel z akcjami wiersza. Otwarcie panelu jednego wiersza zamyka pozostałe.',
        s9f2_edit: 'ikona <code>edit_square.png</code>, przekierowuje do <code>edit.php</code>.',
        s9f2_dup: 'ikona <code>content_copy.png</code>, klonuje wiersz przez <code>mass_duplicate</code> i przeładowuje siatkę. Bez potwierdzenia.',
        s9f2_delete: 'ikona <code>delete.png</code> (czerwony hover), wymaga potwierdzenia.',
        s9f2_visible: 'Cała kolumna akcji renderuje się tylko dla ról z prawem zapisu — w praktyce dla roli <strong>Edytor</strong>, ponieważ konta Admin są przekierowywane z frontendu do panelu administracyjnego. Przeglądający widzą siatkę tylko do odczytu, bez kolumny akcji.',
        s9g_head: '9g. Zakładka Wydajność',
        s9g_desc: '<strong>Admin → System → Wydajność</strong> — panel diagnostyczny tylko do odczytu z sześcioma niezależnymi sekcjami.',
        s9g_li1_label: 'Doradca brakujących indeksów',
        s9g_li1: 'Wyszukuje kolumny bez indeksów. Generuje gotowy SQL <code>CREATE INDEX</code>.',
        s9g_li2_label: 'Nieużywane indeksy',
        s9g_li2: 'Indeksy z <code>idx_scan = 0</code>. Generuje SQL <code>DROP INDEX</code>.',
        s9g_li3_label: 'Analizator wolnych zapytań',
        s9g_li3: 'Odczytuje <code>pg_stat_statements</code>. Wyróżnia zapytania powyżej 100 ms / 500 ms.',
        s9g_li4_label: 'Statystyki tabel i fragmentacja',
        s9g_li4: 'Martwe wiersze, % fragmentacji, liczby skanów, czasy ostatniego vacuum.',
        s9g_li5_label: 'Stan bazy danych',
        s9g_li5: 'Współczynnik trafień cache, połączenia, zakleszczenia, rozmiar bazy, wersja PG.',
        s9g_li6_label: 'Ostrzeżenia konfiguracji schematu',
        s9g_li6: 'Flagi dla tabel &gt;20 kolumn, brakujących limitów, sortowania, podtabel bez columns_to_show.',
        s9h_head: '9h. Zakładka Cron powiadomień',
        s9h_desc: '<strong>Admin → System → Cron powiadomień</strong> — sześciosekcyjny interfejs zarządzania cronem powiadomień.',
        s9h_li1_label: 'Ręczne uruchomienie',
        s9h_li1: 'Wykonuje <code>cron/cron_notifications.php</code> natychmiastowo.',
        s9h_li2_label: 'Historia uruchomień',
        s9h_li2: 'Ostatnie 50 wpisów z <code>spw_users_notifications_log</code>.',
        s9h_li3_label: 'Statystyki powiadomień',
        s9h_li3: 'Łączne, nieprzeczytane, na dziś, nadchodzące. Top 10 użytkowników według liczby nieprzeczytanych.',
        s9h_li4_label: 'Konfiguracja crona',
        s9h_li4: 'Gotowe do skopiowania komendy dla Linux/macOS, Windows Task Scheduler i Docker.',
        s9h_li5_label: 'Czyszczenie dziennika',
        s9h_li5: 'Usuwa wpisy <code>spw_users_notifications_log</code> starsze niż N dni.',
        s9h_li6_label: 'E-mail',
        s9h_li6: 'Adres "Od" dla kolejkowanych maili automatyzacji oraz opcjonalna wysyłka przez uwierzytelnione SMTP (host, port, szyfrowanie, login, hasło) z przyciskiem testu połączenia. Hasło jest przechowywane zaszyfrowane i nigdy nie jest pokazywane w interfejsie.',
        s9i_head: '9i. Rozmiar strony siatki',
        s9i_admin_label: 'Domyślny administrator',
        s9i_admin: 'Schemat → <em>Globalne ustawienia siatki</em> → <em>Domyślny rozmiar strony</em> (10 / 25 / 50 / 100).',
        s9i_user_label: 'Nadpisanie przez użytkownika',
        s9i_user: 'Selektor <em>Wierszy na stronie</em> w pasku paginacji, zapisywany w <code>localStorage</code>.',
        s9i_priority_label: 'Priorytet',
        s9i_fallback: 'awaryjnie',
        s9j_head: '9j. Relacje wiele-do-wielu',
        s9j_desc: 'Relacje M2M łączą rekordy poprzez <strong>tabelę łącznikową</strong>. Renderowane jako panel pól wyboru w formularzach edycji i tworzenia.',
        s9j_how_head: 'Jak działa',
        s9j_how1_label: 'Utwórz tabelę łącznikową w PostgreSQL',
        s9j_how1: 'z dwiema kolumnami FK i ograniczeniem UNIQUE.',
        s9j_how2_label: 'Skonfiguruj relację',
        s9j_how2: 'Schemat → tabela nadrzędna → <em>Relacje wiele-do-wielu</em> → <strong>+ Dodaj wiele-do-wielu</strong>.',
        s9j_how3_label: 'Zapisz plik',
        s9j_how3: '— panel pól wyboru pojawia się automatycznie.',
        s9j_config_head: 'Pola konfiguracji',
        s9j_config_li: 'Etykieta wyświetlana, Tabela łącznikowa, Własny FK, Obcy FK, Inna tabela, Kolumna wyświetlana.',
        s9j_runtime_head: 'Zachowanie w czasie wykonania',
        s9j_runtime1: 'Przy zapisie: wszystkie istniejące wiersze łącznikowe są usuwane i nowe wiersze wstawiane atomowo w jednej transakcji PostgreSQL.',
        s9j_runtime2: 'Rola Przeglądającego widzi pola wyboru w stanie wyłączonym.',
        s9k_head: '9k. Mapa schematu (ERD)',
        s9k_desc: '<strong>Zarządzanie danymi → Schemat → Mapa schematu</strong> renderuje interaktywny diagram ERD z <code>spw_config.schema</code>. Automatyczny układ oparty na sile. Brak zewnętrznych bibliotek.',
        s9k_types_label: 'Typy połączeń',
        s9k_types: 'Klucz obcy (ciągła szara), Podtabela (przerywana granatowa), Wiele-do-wielu (kropkowana szara) — zgodnie z legendą na kanwie. Licznik nad kanwą pokazuje, ile tabel i ile połączeń każdego rodzaju narysowano.',
        s9k_controls_label: 'Sterowanie',
        s9k_controls: 'Przesuwanie, zoom, przeciąganie tabel, klik do podświetlenia, wyszukiwanie, pokaż/ukryj ukryte tabele, Dopasuj widok, Eksport PNG.',
        s10_head: '10. Moduł plików',
        s10_desc: 'Centralne repozytorium dokumentów i multimediów wspierane przez <code>spw_files</code>. Zakładka jest jednocześnie przeglądarką plików i ekranem konfiguracji modułu.',
        s10_browse_label: 'Przeglądanie',
        s10_browse: 'Stronicowana lista (domyślnie 25 wierszy na stronę, <code>FILES_PAGE_LIMIT</code>) z wyszukiwaniem po nazwie, nazwie wyświetlanej i tagach, filtrem typu pliku oraz sortowaniem kliknięciem po typie, nazwie, nazwie wyświetlanej, tagach, rozmiarze, powiązanym rekordzie i dacie przesłania.',
        s10_meta_label: 'Metadane pliku',
        s10_meta: 'Nazwa wyświetlana i tagi są edytowalne w miejscu. Usuwanie jest <strong>miękkie</strong> — wiersz zachowuje znacznik <code>deleted_at</code> i znika z list, zamiast zostać usunięty.',
        s10_bulk_label: 'Operacje masowe',
        s10_bulk: 'Zaznacz kilka plików, aby usunąć je jednym wywołaniem lub nadać im wspólne tagi; pojedyncze żądanie jest ograniczone do 500 plików.',
        s10_config_label: 'Konfiguracja',
        s10_config: 'Maksymalny rozmiar pliku, dozwolone typy, dozwolone rozszerzenia, ścieżka przechowywania (nie może być dostępna przez www — pliki są przesyłane przez <code>file_download.php</code>). Panel administracyjny zapisuje tę konfigurację dosłownie, więc lista rozszerzeń jest po stronie serwera przecinana ze zbiorem rozszerzeń, których treść uploader potrafi zweryfikować — wpis niemożliwy do weryfikacji zostanie odrzucony przy wysyłce niezależnie od konfiguracji.',
        s10_relations_label: 'Relacje z rekordami',
        s10_relations: 'Opcjonalnie powiąż przesłane pliki z konkretnymi wierszami w tabeli docelowej.',
        s10b_head: '10b. Edytor Widoków',
        s10b_desc: '<strong>Zarządzanie danymi → Widoki</strong> udostępnia widoki PostgreSQL tylko do odczytu jako przeglądane strony siatki, obok zwykłych tabel. Wykrywane i konfigurowane są tak samo widoki zwykłe, jak i <strong>zmaterializowane</strong>.',
        s10b_matview_label: 'Widoki zmaterializowane',
        s10b_matview: 'Widok zmaterializowany zwraca wiersze zapisane przy ostatnim odświeżeniu, a nie dane na żywo — OpenSparrow tylko go czyta i nigdy nie odświeża za Ciebie. Utrzymuj go aktualnym przez <code>REFRESH MATERIALIZED VIEW schema.nazwa</code> z zadania bazodanowego lub wpisu crona, inaczej strona po cichu pokaże nieaktualne liczby. Sync wypisuje tylko relacje, do których użytkownik bazy danych aplikacji ma prawo <code>SELECT</code>, więc widok niemożliwy do odczytania nigdy nie zostanie zaproponowany.',
        s10b_sync_label: 'Synchronizacja',
        s10b_sync: '"↻ Sync PostgreSQL Views" wykrywa widoki w przeszukiwanym schemacie (schematach) oraz metadane ich kolumn; wykryte widoki są scalane z <code>spw_config.views</code> bez usuwania istniejących ustawień widoku (ikona, nazwa w menu, drill-down).',
        s10b_schemas_label: 'Zakładka Schemas',
        s10b_schemas: 'Domyślnie synchronizacja przeszukuje tylko schemat aplikacji (<code>config/database.json</code> → <code>schema</code> / <code>PGSCHEMA</code>, domyślnie <code>app</code>). Zaznacz dodatkowe schematy (pobierane z <code>information_schema.schemata</code>), aby uwzględnić widoki znajdujące się gdzie indziej, np. w schemacie demo. Wybór jest zapisywany razem z konfiguracją widoków.',
        s10b_display_label: 'Konfiguracja pojedynczego widoku',
        s10b_display: 'Nazwa wyświetlana, nazwa w menu, ikona, widoczność i poziomy drill-down — konfigurowane tak samo jak dla tabel.',
        s10b_readonly_label: 'Tylko do odczytu',
        s10b_readonly: 'Widoków nie można edytować ani usuwać przez siatkę — są tylko do odczytu, jak natywne widoki SQL.',
        s11_head: '11. Podgląd menu i edytor nawigacji',
        s11_desc: 'Renderuje pasek boczny frontendu i pozwala zmieniać kolejność lub zagnieżdżać elementy przez przeciąganie. Każda zmiana jest zapisywana automatycznie.',
        s11_dnd_head: 'Kontrolki przeciągnij i upuść',
        s11_reorder_label: 'Zmiana kolejności',
        s11_reorder: 'Przeciągnij powyżej lub poniżej innego elementu.',
        s11_nest_label: 'Zagnieżdżanie',
        s11_nest: 'Upuść na środkową strefę elementu najwyższego poziomu. Maksymalna głębokość: <strong>1 poziom</strong>.',
        s11_unnest_label: 'Odgnieżdżanie',
        s11_unnest: 'Przeciągnij element podrzędny na poziom główny.',
        s11_autosave_label: 'Automatyczny zapis',
        s11_autosave: 'Każde upuszczenie wyzwala zapis do <code>spw_config.menu</code> po 350 ms opóźnienia.',
        s11b_head: '11b. Systemy demo (szablony szybkiego startu)',
        s11b_desc: '<strong>System → Systemy demo</strong> udostępnia gotową aplikację demo: CRM.',
        s11b_what_label: 'Co jest instalowane',
        s11b_what: 'Schemat PostgreSQL, dane testowe, wpisy schematu aplikacji, widgety dashboardu, źródła kalendarza, przepływy pracy, widoki SQL.',
        s11b_safety_label: 'Bezpieczeństwo',
        s11b_safety: 'Zarówno instalacja, jak i deinstalacja wymagają wpisania <code>CONFIRM</code>.',
        s11b_cleanup_label: 'Czyszczenie przy deinstalacji',
        s11b_cleanup: 'Schemat demo jest usuwany (CASCADE). Wpisy konfiguracyjne są czyszczone, jeśli zawierają tylko treści demo.',
        s11b_demo1_head: 'Demo 1: CRM',
        s11b_demo1_text: 'Firmy, kontakty, szanse sprzedaży, oferty, faktury, zasoby, aktywności. Klucze obce z podtabelami, kodowanie kolorami etapów sprzedaży, drill-down przychodów rok → miesiąc, tablica Kanban szans, automatyzacje (w tym e-mail powitalny), reguły anonimizacji RODO dla starych leadów.',
        s11c_head: '11c. Import CSV',
        s11c_desc: 'Zarządzanie danymi → Import CSV — masowy import wierszy z pliku .csv do dowolnej tabeli skonfigurowanej w schemacie. Generator PHP odczytuje plik wierszami (bez limitu pamięci); wstawiane są paczkami po max 1 000 wierszy w transakcji.',
        s11c_step1_label: 'Krok 1 — Wybierz tabelę i prześlij plik',
        s11c_step1: 'Wybierz tabelę docelową z listy, następnie przeciągnij i upuść (lub kliknij) plik .csv (max 50 MB). Plik jest walidowany po stronie serwera przez prawdziwy typ MIME, nie tylko rozszerzenie.',
        s11c_parse_label: 'Separator i kodowanie',
        s11c_parse: 'Separator: przecinek, średnik, tabulator (TSV) lub pionowa kreska. Kodowanie: UTF-8, Windows-1250, Windows-1252, ISO-8859-1, ISO-8859-2 lub Windows-1251. Oba są zapamiętywane w <code>localStorage</code> między importami, a lokalny podgląd ponownie parsuje plik z ich użyciem.',
        s11c_create_label: 'Tworzenie nowej tabeli z CSV',
        s11c_create: 'Zamiast wybierać istniejącą tabelę, zaznacz <em>Create new table from CSV</em>: typy kolumn są proponowane na podstawie przykładowych wartości z pliku, podajesz nazwę tabeli i jej schemat, a tabela jest tworzona i rejestrowana przed wczytaniem wierszy. Wpisana nazwa jest normalizowana do małych liter, a znaki niealfanumeryczne zamieniane na podkreślenia.',
        s11c_mode_label: 'Tryby importu',
        s11c_mode: '<strong>Tryb normalny</strong> — wsadowy INSERT w transakcjach do 1 000 wierszy; śledzi błędy pojedynczych wierszy i obsługuje upsert. <strong>Szybki tryb COPY</strong> — przesyła plik bezpośrednio do PostgreSQL przez <code>COPY FROM STDIN</code>, 10-60x szybciej przy dużych plikach, ale <strong>bez śledzenia błędów pojedynczych wierszy</strong> (jedno niezgodne typowo pole wycofuje cały import) i <strong>bez upsertu</strong> — kolumna konfliktu jest ukrywana po jego wybraniu.',
        s11c_step2_label: 'Krok 2 — Mapuj kolumny',
        s11c_step2: 'Każdy nagłówek CSV jest wyświetlany z przykładowymi wartościami. Użyj listy, aby zmapować go na kolumnę bazy danych, lub zostaw "Pomiń". Automatyczne mapowanie wstępnie zaznacza kolumny o pasujących nazwach (bez uwzględnienia wielkości liter).',
        s11c_upsert_label: 'Upsert / obsługa konfliktów',
        s11c_upsert: 'Wybierz kolumnę konfliktu (np. <code>id</code>). Przy konflikcie pasujący wiersz jest aktualizowany zamiast odrzucany. Kolumna musi mieć ograniczenie UNIQUE i musi być uwzględniona w mapowaniu.',
        s11c_types_label: 'Rzutowanie typów',
        s11c_types: 'Wartości są rzutowane przed wstawieniem: daty akceptują <code>dd.mm.yyyy</code>, <code>dd/mm/yyyy</code>, <code>yyyy-mm-dd</code>; wartości logiczne akceptują <code>true/false/1/0/yes/y/t</code>; liczby obsługują przecinek jako separator dziesiętny.',
        s11c_errors_label: 'Obsługa błędów',
        s11c_errors: 'Błędy rzutowania poszczególnych wierszy i wycofania paczek DB są wychwytywane. Pominięte wiersze są zapisywane do <code>spw_import_rows_log</code>; import kontynuuje pracę. Status to <code>failed</code> tylko gdy pominięto wszystkie wiersze.',
        s11c_tables_label: 'Tabele systemowe',
        s11c_tables: '<code>spw_imports</code> — jeden wiersz na import (użytkownik, plik, tabela, status, liczniki). <code>spw_import_rows_log</code> — szczegóły pominiętych wierszy (numer, surowy JSON, komunikat błędu). Aktywowane przez <em>Zastosuj oczekujące migracje</em>.',
        s11c_history_label: 'Historia importów',
        s11c_history: 'Dolna część strony wyświetla ostatnie 100 importów. Kliknij <em>Log</em> przy wierszu z pominiętymi rekordami, aby rozwinąć tabelę błędów inline.',
        s11d_head: '11d. Import ETL',
        s11d_desc: '<strong>Zarządzanie danymi → ETL</strong> — zaplanowany lub uruchamiany na żądanie import wsadowy z zewnętrznej bazy źródłowej (MySQL, MariaDB, PostgreSQL, SQLite) lub pliku CSV pobieranego przez FTP/FTPS, do lokalnej tabeli docelowej PostgreSQL. Niezależny od usuniętej funkcji MySQL Gateway; łączy się przez warstwę PDO opartą na sterownikach, rozszerzalną o Oracle/DB2/SQL Server.',
        s11d_conn_label: 'Źródła',
        s11d_conn: 'Zdefiniuj jedno lub więcej nazwanych źródeł. Źródła bazodanowe (MySQL, MariaDB, PostgreSQL) wymagają hosta, portu, bazy, użytkownika i hasła; SQLite zamiast hosta przyjmuje ścieżkę do czytelnego pliku; źródło <em>CSV file (FTP/FTPS)</em> przyjmuje protokół, host, katalog zdalny, nazwę pliku, separator, flagę nagłówka i tryb pasywny. <em>Test connection</em> weryfikuje osiągalność przed zapisem. Przechowywane w <code>spw_config.etl</code>; hasło jest maskowane (<code>********</code>) po zapisaniu i zastępowane tylko po ponownym wpisaniu.',
        s11d_jobs_label: 'Zadania',
        s11d_jobs: 'Każde zadanie wskazuje źródło, z którego czyta, oraz definiuje tabelę docelową i opcjonalne mapowanie kolumn (źródło → cel; dopasowywane automatycznie po nazwie, gdy pominięte). Zadania ze źródeł bazodanowych mają dodatkowo zapytanie źródłowe (pojedynczy, tylko do odczytu <code>SELECT</code>/<code>WITH</code> — DML/DDL i wiele instrukcji są odrzucane); zadania ze źródła CSV/FTP nie mają zapytania i importują cały plik.',
        s11d_modes_label: 'Tryby ładowania',
        s11d_modes: '<strong>Pełne odświeżenie</strong> (truncate + insert), <strong>Dopisywanie</strong> (tylko insert), <strong>Upsert</strong> (insert z aktualizacją <code>ON CONFLICT</code> po jednej lub kilku kolumnach klucza).',
        s11d_incremental_label: 'Ładowanie przyrostowe',
        s11d_incremental: 'Ustaw kolumnę przyrostową i użyj placeholdera <code>{{watermark}}</code> w zapytaniu źródłowym; silnik podstawia ostatnio zapisaną wartość (lub skonfigurowaną wartość początkową) i zapisuje nowy znacznik po każdym udanym uruchomieniu.',
        s11d_preview_label: 'Podgląd',
        s11d_preview: 'Uruchamia zapytanie źródłowe na skonfigurowanym połączeniu i pokazuje do 20 przykładowych wierszy i kolumn przed zapisaniem zadania.',
        s11d_schedule_label: 'Harmonogram',
        s11d_schedule: '<em>Ręcznie</em>, <em>Codziennie</em>, <em>Co tydzień</em> lub <em>Co miesiąc</em>, wymuszane przez <code>cron/cron_etl.php</code>. <em>Run Now</em> uruchamia natychmiast wszystkie włączone zadania niezależnie od harmonogramu.',
        s11d_log_label: 'Historia uruchomień',
        s11d_log: 'Ostatnie 50 uruchomień z <code>spw_etl_log</code> pokazuje liczbę odczytanych/zapisanych wierszy, czas trwania i ewentualny błąd. Log Cleanup usuwa wpisy starsze niż N dni.',
        s11e_head: '11e. Przepływy ETL (Flows)',
        s11e_desc: 'Zakładka <strong>Zarządzanie danymi → ETL → Flows</strong> — łączy istniejące zadania ETL w uporządkowaną sekwencję (kafelki: start, zadanie, zadanie, ..., koniec). Kroki odwołują się do identyfikatorów zadań z zakładki Zadania, więc edycja zadania zmienia też działanie każdego przepływu, który się do niego odwołuje. Przechowywane w <code>spw_config.etl_flows</code>, osobno od klucza <code>etl</code>.',
        s11e_run_label: 'Wykonanie',
        s11e_run: 'Kroki wykonywane są ściśle w kolejności, a przepływ zatrzymuje się natychmiast po pierwszym nieudanym kroku — pozostałe kroki się nie wykonują. <em>Run Now</em> uruchamia przepływ natychmiast, z pominięciem harmonogramu.',
        s11e_schedule_label: 'Harmonogram',
        s11e_schedule: 'Ten sam model <em>Ręcznie</em>/<em>Codziennie</em>/<em>Co tydzień</em>/<em>Co miesiąc</em> co dla zadań, wymuszany przez <code>cron/cron_etl_flow.php</code>, pilnowany osobno dla każdego przepływu (nie globalnie dla modułu).',
        s11e_log_label: 'Historia uruchomień',
        s11e_log: 'Każdy przepływ pokazuje własną historię uruchomień: jeden wiersz na uruchomienie w <code>spw_etl_flow_run_log</code> (ogólny status, który krok zawiódł) oraz szczegóły poszczególnych kroków w <code>spw_etl_flow_step_log</code>.',
        s13_head: '13. Moduł wielojęzyczny (i18n)',
        s13_desc: 'OpenSparrow obsługuje wiele języków interfejsu poprzez płaskie pliki JSON z tłumaczeniami. Aktywny język jest rozwiązywany per sesja; szablony PHP i wszystkie moduły JS korzystają z tego samego pakietu.',
        s13_config_head: 'Konfiguracja — spw_config.settings',
        s13_config1: '<code>"default_language": "pl"</code> — język używany gdy brak preferencji w sesji.',
        s13_config2: '<code>"available_languages": ["en", "pl"]</code> — włącza przełącznik języka; lista akceptowanych kodów locale.',
        s13_trans_head: 'Pliki tłumaczeń',
        s13_trans_desc: 'Każde locale ma jeden plik: <code>languages/{locale}.json</code> (np. <code>languages/pl.json</code>). Struktura:',
        s13_trans1: 'Klucze najwyższego poziomu to przestrzenie nazw: <code>common</code>, <code>grid</code>, <code>form</code>, <code>auth</code>, <code>header</code>, <code>admin</code>, <code>pagination</code>, <code>filter</code>, <code>files</code>, <code>images</code>, <code>notifications</code>, <code>notes</code>, <code>dashboard</code>, <code>workflow</code>, <code>comments</code>, <code>owners</code>, <code>views</code>, <code>calendar</code>, <code>board</code>, <code>print</code>, <code>data_cleanup</code>, <code>mass_edit</code>, <code>mass_owner</code>, <code>mass_duplicate</code>, <code>mass_delete</code>, <code>shortcuts</code>, <code>agent</code>, <code>setup</code>. Jeden dodatkowy klucz najwyższego poziomu, <code>_meta</code>, nie jest przestrzenią nazw: zawiera własną <code>name</code> i kierunek tekstu <code>dir</code> danej lokalizacji.',
        s13_trans2: 'Wartości liczby mnogiej to obiekty: <code>{"one": "...", "few": "...", "many": "..."}</code> dla polskiego; <code>{"one": "...", "other": "..."}</code> dla angielskiego.',
        s13_trans3: 'Zmienne używają składni <code>{name}</code>: np. <code>"showing": "Wyniki {from}–{to} z {total} rekordów"</code>.',
        s13_trans4_label: 'Krytyczne',
        s13_trans4: 'Cudzysłów wewnątrz wartości JSON musi być poprzedzony ukośnikiem: <code>\\"</code>. Nieescapowany <code>"</code> powoduje ciche niepowodzenie <code>json_decode</code> i powrót do języka angielskiego bez żadnego komunikatu błędu.',
        s13_php_head: 'API PHP',
        s13_php1: '<code>includes/i18n.php</code> — ładowany przez <code>includes/session.php</code>, dostępny wszędzie.',
        s13_php2: '<code>I18n::locale()</code> — zwraca aktywne locale (np. <code>\'pl\'</code>). Używaj w <code>&lt;html lang="..."&gt;</code>.',
        s13_php3: '<code>t($key, $vars = [], $count = null)</code> — tłumaczy klucz w notacji kropkowej; zastępuje placeholdery <code>{name}</code>; wybiera formę liczby mnogiej gdy podany <code>$count</code>.',
        s13_php4: 'Pakiet tłumaczeń serwowany do JS przez <code>api.php?action=i18n_bundle</code> jako płaski JSON klucz→wartość.',
        s13_js_head: 'API JavaScript',
        s13_js1: '<code>assets/js/i18n.js</code> — singleton ES module. Import: <code>import { I18n } from \'./i18n.js\';</code>',
        s13_js2: 'Zawsze wywołuj <code>await I18n.load()</code> przed renderowaniem przetłumaczonego tekstu — komponenty renderowane przed rozwiązaniem <code>load()</code> pokażą surowe klucze.',
        s13_js3: '<code>I18n.t(\'common.save\')</code> — podstawowe wyszukiwanie.',
        s13_js4: '<code>I18n.t(\'grid.showing\', { from: 1, to: 10, total: 42 })</code> — z podstawianiem zmiennych.',
        s13_js5: '<code>I18n.t(\'files.count\', { count: 3 }, 3)</code> — z wyborem formy liczby mnogiej.',
        s13_js6: 'Buduj węzły DOM przez <code>el.textContent = I18n.t(...)</code> — nigdy nie wstrzykuj tłumaczeń przez <code>innerHTML</code>.',
        s13_add_head: 'Dodawanie nowego języka',
        s13_add1: 'Skopiuj <code>languages/en.json</code> → <code>languages/{locale}.json</code>.',
        s13_add2: 'Przetłumacz wszystkie wartości; zaktualizuj formy liczby mnogiej zgodnie z zasadami docelowego języka.',
        s13_add3: 'Dodaj kod locale do <code>"available_languages"</code> w <code>spw_config.settings</code>.',
        s13_add4: 'Zwaliduj JSON przed wdrożeniem: <code>node -e "JSON.parse(require(\'fs\').readFileSync(\'languages/{locale}.json\',\'utf8\'))"</code>.',
        s13_docslang_label: 'Języki dokumentacji',
        s13_docslang: 'Interfejs aplikacji jest tłumaczony na wszystkie języki obecne w katalogu <code>languages/</code>, ale ta dokumentacja panelu administracyjnego jest utrzymywana wyłącznie po angielsku i po polsku — pasek języków na górze strony oferuje EN i PL. Nie dodawaj kolejnych wersji językowych dokumentacji; przy zmianie sekcji aktualizuj obie wersje.',
        s12_head: '12. Uwagi dotyczące wdrożenia',
        s12_li1: '<strong>Udostępniaj tylko <code>public/</code>:</strong> ustaw katalog główny (document root) serwera WWW na katalog <code>public/</code>. Cały kod backendu, konfiguracja i pliki znajdują się powyżej niego i są nieosiągalne przez HTTP; reguły <code>.htaccess</code> <code>Deny from all</code> w poszczególnych folderach pozostają jako dodatkowe zabezpieczenie.',
        s12_li2: '<strong>Uprawnienia do przechowywania:</strong> <code>config/</code> i <code>storage/</code> muszą być zapisywalne przez użytkownika serwera www.',
        s12_li3: '<strong>Kopie zapasowe:</strong> Regularnie wykonuj migawki <code>pg_dump</code> — obejmują też konfigurację, bo ta żyje w <code>spw_config</code>.',
        s12_li4: '<strong>Tryb demo:</strong> Ustaw <code>DEMO_MODE=true</code>, aby zablokować wszystkie operacje zapisu w API administracyjnym.',
        s12_env_head: 'Zmienne środowiskowe',
        s12_th_var: 'Zmienna',
        s12_th_default: 'Domyślnie',
        s12_th_desc: 'Opis',
        env_appenv: 'Środowisko uruchomieniowe.',
        env_dbhost: 'Host PostgreSQL.',
        env_dbport: 'Port PostgreSQL.',
        env_timezone: 'Strefa czasowa IANA dla każdej sesji PostgreSQL.',
        env_cookies: 'Ustaw <code>false</code> przy zwykłym HTTP.',
        env_session: 'Bezwzględny czas wygaśnięcia sesji w sekundach (8 h).',
        env_none: 'brak',
        env_iphash_req: 'Wymagane na produkcji.',
        env_iphash: 'Sekret HMAC dla pseudonimizacji IP.',
        env_ip_attempts: 'Próg nieudanych logowań na IP.',
        env_user_attempts: 'Próg nieudanych logowań na nazwę użytkownika.',
        env_lockout: 'Czas blokady w minutach.',
        env_demo: 'Zablokuj wszystkie operacje zapisu w API administracyjnym.',
        env_files: 'Domyślny limit rozmiaru przesyłania.',
        env_snapshots: 'Włącz migawki rekordów w całym systemie.',
        env_pgschema: 'Schemat dla tabel <code>spw_*</code>.',
        env_autogen: 'generowany automatycznie',
        env_enckey: 'Klucz symetryczny szyfrujący sekrety przechowywane w <code>spw_config</code> (sekrety podpisu webhooków, hasło SMTP, klucz API Ollama Cloud). Gdy nie jest ustawiony, losowy klucz jest generowany raz i zapisywany do <code>includes/.secret_key</code>. <strong>Ustaw go jawnie na każdym wdrożeniu, gdzie ten plik nie jest trwały</strong> — kontenery lub wiele węzłów aplikacji, z których każdy wygeneruje własny klucz, nie odszyfrują nawzajem swoich sekretów.',
        env_proxy: 'Czy <code>X-Forwarded-For</code> jest zaufany przy ustalaniu IP klienta na potrzeby limitowania logowań. Ustaw <code>false</code>, gdy aplikacja jest dostępna bezpośrednio, aby podrobiony nagłówek nie omijał limitera.',
        env_samesite: 'Polityka SameSite dla ciasteczka sesji.',
        env_savepath: 'Katalog przechowywania sesji. Gdy nieustawiony, używany jest projektowy <code>storage/sessions</code>.',
        env_hsts: 'Max-age nagłówka HSTS, w sekundach.',
        env_mailfrom: 'Adres „From" dla kolejkowanych e-maili automatyzacji; ustawiany także w zakładce Cron Notifications. Wysyłka e-maili pozostaje wyłączona, dopóki żaden z tych dwóch nie zostanie ustawiony.',
        s12_env_note: 'Ta tabela obejmuje zmienne najważniejsze przy wdrożeniu. Istnieją też inne parametry (limity rozmiaru stron, czasy cache, limity zapytań RAG, rozmiary miniatur); każdy jest odczytywany przez <code>get_env()</code> w <code>includes/config.php</code>, który jest listą autorytatywną.',
        sRag_head: '15. RAG (Retrieval-Augmented Generation)',
        sRag_desc: 'Lokalna baza wiedzy zasilana przez Ollama. Prześlij dokumenty .txt i odpytuj je za pomocą odpowiedzi wygenerowanych przez AI na podstawie Twoich danych.',
        s13_docs_label: 'Prześlij dokumenty',
        s13_docs: 'Przejdź do <strong>Admin → Centrum AI → Dokumenty</strong>. Prześlij pliki .txt, oznacz je tagami (np. <em>legal</em>, <em>faq</em>) i opcjonalnie przypisz język. Dokumenty są przechowywane w <code>spw_rag_files</code> i indeksowane do wyszukiwania pełnotekstowego.',
        s13_config_label: 'Skonfiguruj Ollama',
        s13_config: 'Przejdź do karty <strong>Ustawienia</strong>. Wybierz swoją konfigurację Ollama: <ul style="padding-left:20px;margin-top:8px;"><li><strong>Lokalne Ollama:</strong> URL <code>http://localhost:11434</code> (domyślnie). Pobierz i uruchamiaj modele na swoim komputerze.</li><li><strong>Ollama Cloud:</strong> URL <code>https://api.ollama.com</code>. Wymaga klucza API z Twojego konta Ollama Cloud. Wklej go w polu Klucz API.</li></ul> Wybierz model z listy. Kliknij <strong>Testuj i załaduj modele</strong>, aby zweryfikować połączenie i wyświetlić dostępne modele.',
        s13_test_label: 'Testuj zapytania',
        s13_test: 'Użyj karty <strong>Test</strong>, aby wysyłać pytania do bazy wiedzy. Opcjonalnie filtruj po tagach lub wybierz język odpowiedzi. Wynik pokazuje wygenerowaną odpowiedź i które dokumenty zostały dopasowane.',
        s13_stats_label: 'Statystyki',
        s13_stats: 'Wyświetl historię zapytań, liczby tokenów i czasy odpowiedzi na karcie <strong>Statystyki</strong>. Śledź wydajność Ollama i trafność dokumentów w czasie.',
        s13_multilang_label: 'Obsługa wielojęzyczności',
        s13_memory_label: 'Pamięć konwersacji',
        s13_memory: 'Ustaw <strong>Conversation memory (turns)</strong> w zakładce <strong>Ustawienia</strong> (0–10, domyślnie <code>0</code> = wyłączone). Panel Ask AI we front-endzie odsyła poprzednie pytanie i poprzednią odpowiedź, dzięki czemu pytania uzupełniające („a za ostatni miesiąc?") zachowują kontekst. Wysyłana jest tylko ostatnia tura — nigdy cały wątek — a serwer przycina ją do skonfigurowanego limitu. Model ma jej używać wyłącznie do ustalenia, do czego odnosi się pytanie eliptyczne, nigdy jako źródła faktów. Odmowa odpowiedzi nie jest zapamiętywana (podanie z powrotem „nie ma tego w kontekście" tylko wymusza kolejną odmowę), a przycisk <em>Wyczyść</em> kasuje pamięć całkowicie.',
        s13_multilang: 'Zapytania automatycznie odpowiadają w bieżącym języku interfejsu użytkownika. Opcjonalnie oznacz dokumenty kodami języka (np. <em>lang:pl</em>, <em>lang:en</em>) dla organizacji. Nie wymagana zmiana schematu.',
        sRagAgg_label: 'Widoki agregujące',
        sRagAgg_summary: 'Podepnij widok SQL pod tabelę, żeby asystent mógł odpowiadać na pytania o liczności/sumy/średnie dokładnymi liczbami z całej tabeli, nie tylko z widocznej strony gridu — konfiguracja na karcie <strong>Ustawienia</strong>.',
        sRagAgg_head: 'Widoki agregujące',
        sRagAgg_desc: 'Kontekst strony gridu wysyłany do asystenta jest ograniczony do bieżącej strony (max 50 wierszy). Jego pierwsza linia mówi, jaką część tabeli obejmuje: jeśli na stronie znalazły się wszystkie pasujące rekordy, asystent dostaje o tym informację i może sam policzyć sumy z tych wierszy, ale przy niepełnej stronie liczenia sum wprost mu zabroniono. Żeby poprawnie odpowiadać na pytania typu "ile jest" lub "jaka jest suma", napisz widok SQL z dokładnie takimi agregatami, jakich potrzebujesz (np. <code>public.v_companies_aggr</code>) bezpośrednio w bazie, a następnie podepnij go pod tabelę w <strong>Centrum AI → Ustawienia → Widoki agregujące</strong>, podając zawsze pełną kwalifikowaną nazwę <code>schemat.widok</code>. Asystent odpytuje widok wyłącznie do odczytu i dostaje z powrotem gotowe liczby — nigdy nie widzi ani nie pisze SQL-a, nigdy nie wybiera, które kolumny agregować.',
        sRagAgg_li1: '<strong>Używaj pełnych, opisowych nazw kolumn</strong> — <code>total_companies</code>, <code>avg_contract_value_pln</code>, nie <code>cnt</code> czy <code>val</code>. Nazwa kolumny to jedyna etykieta, jaką model ma dla liczby obok niej.',
        sRagAgg_li2: '<strong>Wypisz jednostkę lub walutę w nazwie</strong>, jeśli nie jest oczywista — <code>_pln</code>, <code>_days</code>, <code>_hours</code>.',
        sRagAgg_li3: '<strong>Nadaj każdemu agregatowi unikalną, konkretną nazwę</strong> — unikaj generycznych <code>sum1</code>/<code>sum2</code>, gdy widok liczy kilka sum.',
        sRagAgg_li4: '<strong>Używaj identyfikatorów snake_case, po angielsku</strong>, spójnie z resztą schematu tabel.',
        sRagAgg_li5: '<strong>Jeśli widok grupuje wiersze</strong> (<code>GROUP BY</code> po statusie, miesiącu itd.), dodaj wartość grupy jako osobną kolumnę, żeby przy kilku zwróconych wierszach dało się je od siebie odróżnić w odpowiedzi.',
        sRagAgg_li6: '<strong>Widok musi być tylko do odczytu</strong> — jest odpytywany bez parametrów (<code>SELECT * FROM widok LIMIT n</code>) przy każdym pasującym pytaniu, więc nie może mieć efektów ubocznych ani kosztownych złączeń, które spowalniałyby każde zapytanie. Limit <code>n</code> pochodzi z ustawienia <strong>Aggregate view rows</strong> w globalnej konfiguracji RAG (domyślnie 100); jeśli widok zwraca więcej wierszy, blok zostanie obcięty, a asystent odpowie "nie ma tego w kontekście" dla pominiętych wierszy.',
        sRagAgg_li7: '<strong>Zawsze podawaj pełną kwalifikowaną nazwę</strong> — <code>schemat.widok</code> (np. <code>public.v_companies_aggr</code>, <code>spw_crm.v_deals_aggr</code>). Nie ma domyślnego schematu; sama nazwa widoku jest odrzucana.',
        sRagAgg_li8: '<strong>Można podpiąć także widok zmaterializowany</strong> — lista podpowiedzi oznacza go dopiskiem <em>materialized</em>. Odpowiada szybciej, bo agregat jest wyliczony wcześniej, ale zwraca liczby z ostatniego <code>REFRESH MATERIALIZED VIEW</code>; OpenSparrow nigdy go nie odświeża. Podpinaj go tylko wtedy, gdy coś innego utrzymuje go w aktualności — inaczej asystent poda nieaktualne sumy z pełnym przekonaniem. Proponowane są wyłącznie relacje, do których użytkownik bazy danych aplikacji ma prawo <code>SELECT</code>.',
        sRagRollup_head: 'Podsumy (ROLLUPS)',
        sRagRollup_desc: 'Widok grupowany po dwóch rzeczach naraz (firma <em>i</em> etap) zawiera odpowiedź na pytanie „jaka jest suma dla etapu X" wyłącznie w postaci kilku osobnych wierszy — a modelowi nie wolno samodzielnie dodawać liczb, więc odpowiedziałby poprawnie, ale bezużytecznie: „nie ma tego w kontekście". Dlatego serwer agreguje widok jeszcze przed wysłaniem pytania: grupuje wiersze po każdej kolumnie o małej liczbie wartości z osobna i dokłada dokładne podsumy jako blok <code>ROLLUPS</code>, który model ma już tylko zacytować. Nie wymaga to żadnej konfiguracji i działa dla każdego podpiętego widoku.',
        sRagRollup_li1: '<strong>Sumowane są tylko kolumny addytywne</strong> — kolumna trafia do podsumy, gdy każda jej wartość jest liczbą, a nazwa nie zapowiada statystyki nieaddytywnej (<code>avg_</code>, <code>min_</code>, <code>max_</code>, <code>first_</code>, <code>last_</code>, <code>_pct</code>, <code>_ratio</code>, <code>id</code>). Średnia ze średnich nigdy nie jest liczona; jeśli widok ma dokładnie jedną kolumnę z licznością, dokładana jest dokładna wartość <code>derived_avg_…</code>.',
        sRagRollup_li2: '<strong>Wygrywają grupowania zgrubne</strong> — używane są maksymalnie trzy kolumny grupujące o 2-25 różnych wartościach, od najbardziej zgrubnej (kolumna etapu z 5 wartościami przed kolumną firmy z 24). Kolumny o wartościach dłuższych niż 80 znaków (zlepek kontaktów, notatka) nigdy nie służą do grupowania; kolumna z inną wartością w niemal każdym wierszu również jest pomijana.',
        sRagRollup_li3: '<strong>Blok ma limit rozmiaru</strong> — około 6 000 znaków i 45 linii, liczony per grupowanie, żeby żadna lista nie została przecięta w połowie. Grupowanie, które się nie mieści, jest pomijane w całości; jego wiersze i tak są w widoku powyżej. Podsumy są też pomijane, gdy sam widok został obcięty ustawieniem <strong>Aggregate view rows</strong> — obejmowałyby wtedy tylko część danych.',
        sRagAgg_warn_label: 'Ograniczenie właściciela:',
        sRagAgg_warn: 'Widok wolno podpiąć wyłącznie pod tabelę bez ograniczenia właściciela na poziomie wiersza — zwykły widok SQL nie ma sesji ani <code>user_id</code>, po którym mógłby filtrować. Próba podpięcia go pod tabelę z ograniczeniem właściciela jest odrzucana zarówno w tym UI, jak i po stronie serwera.',
        sPrint_head: '16. Szablony wydruku',
        sPrint_desc: '<strong>Zarządzanie danymi → Print</strong> — definiuje szablony dokumentów do druku/PDF (oferty, faktury, etykiety) powiązane z tabelą lub widokiem, renderowane przez <code>public/print.php</code>.',
        sPrint_source_label: 'Źródło danych',
        sPrint_source: 'Powiąż szablon z dowolną tabelą lub widokiem PostgreSQL; pola szablonu są wypełniane z kolumn wybranego rekordu.',
        sPrint_layout_label: 'Układ',
        sPrint_layout: 'Nagłówek, powtarzalna sekcja pozycji (dla danych z podtabeli jeden-do-wielu, np. pozycji faktury), stopka oraz obliczane sumy.',
        sPrint_pagination_label: 'Paginacja',
        sPrint_pagination: 'Długie listy pozycji dzielone są automatycznie na strony przy użyciu klas CSS druku <code>.pr-page</code> / <code>.pr-page-footer</code>.',
        sPrint_access_label: 'Drukowanie rekordu',
        sPrint_access: 'Z wiersza siatki lub widoku szczegółowego rekordu wybierz skonfigurowany szablon, aby otworzyć <code>public/print.php?template=...&amp;id=...</code> w nowej karcie, gotowy do okna dialogowego drukowania przeglądarki lub zapisu jako PDF.',
        sPrint_config_label: 'Konfiguracja',
        sPrint_config: 'Szablony są przechowywane w <code>spw_config.print</code>.',
        s9m_head: '9m. Anonimizacja danych (RODO)',
        s9m_desc: '<strong>System → Anonimizacja</strong> — automatyczna zamiana wartości kolumn zawierających dane osobowe na skonfigurowany tekst zastępczy. Prawdziwie zanonimizowane dane nie podlegają przepisom RODO. Konfiguracja w <code>spw_config.anonymization</code>; historia wykonań w <code>spw_anonymization_log</code>. Każdy wpis historii można rozwinąć do raportu zgodności RODO/EROD (JSON, zapisywany w <code>spw_anonymization_report</code>) z przyciskiem pobierania.',
        s9m_tab1_label: 'Zakładka Reguły',
        s9m_tab1: 'Definiuje reguły dla każdej kolumny: wybierz tabelę, kolumnę typu data/znacznik czasu oraz próg wieku (anonimizuj rekordy starsze niż N dni), kolumnę z danymi osobowymi i wartość zastępczą (np. <code>***ANONYMIZED***</code>). Przycisk <em>Podgląd (próba)</em> liczy, ile wierszy obejmie każda reguła, bez modyfikowania danych.',
        s9m_tab2_label: 'Zakładka Harmonogram',
        s9m_tab2: 'Włącz/wyłącz moduł i ustaw częstotliwość: <em>Ręcznie</em>, <em>Codziennie</em>, <em>Co tydzień</em> lub <em>Co miesiąc</em>. Skrypt crona (<code>cron/cron_anonymization.php</code>) sam egzekwuje okno czasowe sprawdzając ostatnie udane uruchomienie. <em>Uruchom teraz</em> wykonuje natychmiastowo bez sprawdzania częstotliwości. Sekcja Cron Setup Guide zawiera gotowe komendy dla Linux, Windows i Docker.',
        s9m_tab3_label: 'Zakładka Sugestie',
        s9m_tab3: '<em>Skanuj schemat</em> porównuje nazwy kolumn ze słownikiem i wyświetla potencjalne kolumny z danymi osobowymi. Przycisk <em>+ Dodaj regułę</em> przy każdym dopasowaniu tworzy regułę bezpośrednio.',
        s9m_tab4_label: 'Zakładka Słownik',
        s9m_tab4: 'Słowa kluczowe rozdzielone przecinkami (np. <code>PESEL, NIP, email, telefon, adres, imię</code>). Używane przez Sugestie do dopasowania podciągu w nazwach kolumn i nazwach wyświetlanych. Sekcja Czyszczenie dziennika usuwa stare wpisy <code>spw_anonymization_log</code> starsze niż N dni.',
        s9m_tab5_label: 'Zakładka Historia',
        s9m_tab5: '50 ostatnich uruchomień z <code>spw_anonymization_log</code>, po jednym wierszu na uruchomienie, ze statusem i czasem trwania. Rozwiń wpis, aby wygenerować i pobrać raport zgodności RODO/EDPB.',
        sLogo_head: '9n. Własne logo',
        sLogo_desc: '<strong>Ustawienia → Własne logo</strong> — zastąp domyślne logo OpenSparrow w nagłówku aplikacji własnym obrazem.',
        sLogo_li1_label: 'Upload',
        sLogo_li1: 'PNG, JPEG lub WEBP, maks. 2 MB. Plik jest walidowany po stronie serwera (sprawdzanie MIME, nie tylko rozszerzenia) i zapisywany w <code>public/assets/img/uploads/</code> pod losową nazwą.',
        sLogo_li2_label: 'Przechowywanie',
        sLogo_li2: 'Ścieżka aktywnego logo zapisywana jest w <code>spw_config.settings</code> (<code>custom_logo_path</code>); poprzedni plik jest automatycznie usuwany przy wgraniu nowego logo.',
        sLogo_li3_label: 'Usuwanie',
        sLogo_li3: 'Kliknij Usuń logo, aby skasować plik i klucz ustawień i przywrócić domyślne logo OpenSparrow.',
        sLogo_li4_label: 'Włączanie/wyłączanie',
        sLogo_li4: 'Niezależny checkbox „Pokaż logo w nagłówku" — odznacz i zapisz, aby całkowicie ukryć logo i wrócić do nagłówka bez logo, bez usuwania wgranego pliku.',
        s1_t_clickstats: 'zarejestrowane kliknięcia w interfejsie (użytkownik, czas, element oraz opcjonalnie tabela i rekord w kontekście). Zapisywane wyłącznie, gdy moduł Statystyki kliknięć jest włączony.',
        s9q_head: '9q. Statystyki kliknięć',
        s9q_desc: '<strong>System → Statystyki kliknięć</strong> rejestruje, w które elementy strony użytkownicy faktycznie klikają. Konfiguracja w <code>spw_config.clickstats</code>; zarejestrowane kliknięcia w <code>spw_clickstats</code>, zapisywane przez <code>public/api/clickstats.php</code>. Wymaga migracji <code>3.3_clickstats</code> (Migracje → Inicjalizuj tabele systemowe).',
        s9q_tab1_label: 'Zakładka Ustawienia',
        s9q_tab1: 'Dwa checkboxy. <em>Enable Click Statistics</em> włącza zbieranie; <em>Record Table And Record</em> dodatkowo zapisuje, jaka tabela i rekord były otwarte — po odznaczeniu każdy wpis ogranicza się do użytkownika, czasu i elementu.',
        s9q_tab2_label: 'Zakładka Dziennik',
        s9q_tab2: 'Zarejestrowane kliknięcia, od najnowszych, po 100 na stronę, z filtrowaniem po elemencie i po użytkowniku oraz zestawieniem Top Elements — 20 najczęściej klikanych elementów. Retencja jest ręczna: <em>Delete Older Than</em> przycina dziennik do okna N dni, a <em>Clear Log</em> czyści <code>spw_clickstats</code> w całości — nie ma workera crona ani automatycznego wygasania.',
        s9q_off_label: 'Gdy wyłączone',
        s9q_off: 'Wyłączone znaczy nieobecne. Przy wyłączonym module żaden skrypt kolektora nie trafia do strony, więc nic się nie pobiera, nie powstaje żaden nasłuch i nie leci żadne żądanie. Endpoint niezależnie sprawdza flagę i odpowiada 204 bez zapisu, więc strona zostawiona otwarta w chwili wyłączenia modułu natychmiast przestaje rejestrować.',
        s9q_privacy_label: 'Co jest zbierane',
        s9q_privacy: 'Kliknięcie w przycisk, link lub element z atrybutem <code>data-stat</code> zapisuje etykietę elementu (wartość <code>data-stat</code>, a w jej braku etykietę wyprowadzoną z id/klasy/tekstu), nazwę skryptu strony oraz id użytkownika. Wartości pól, parametry adresu i treść formularzy nie są rejestrowane. Kliknięcia są buforowane w przeglądarce i wysyłane paczkami przez <code>navigator.sendBeacon</code>, więc zbieranie kosztuje kilka żądań na sesję, a nie jedno na kliknięcie. Nazwa tabeli przysłana przez klienta jest zapisywana tylko wtedy, gdy użytkownik ma do niej dostęp.',
        s9q_labels_label: 'Etykiety mogą zawierać dane rekordu',
        s9q_labels: 'Gdy element nie ma ani <code>data-stat</code>, ani id, etykieta jest wyprowadzana z jego własnego widocznego tekstu (do 40 znaków) — a na liście rekordów tym tekstem jest treść rekordu: nazwa pliku na stronie Pliki, tytuł rekordu w linku wiersza. Odznaczenie <em>Record Table And Record</em> tego nie zmienia; ten przełącznik dotyczy wyłącznie osobnych kolumn tabeli i rekordu. Dodaj elementowi <code>data-stat="…"</code>, aby jednoznacznie ustalić, co ma być dla niego zapisywane.',
        s9l_head: '9l. Edycja masowa i szybkie czyszczenie danych',
        s9l_desc: 'Dwa narzędzia masowe dostępne dla roli <strong>editor</strong> bezpośrednio z siatki danych. Oba zawsze pokazują podgląd przed zapisem, a każda zastosowana zmiana trafia do <code>spw_users_log</code>.',
        s9l_edit_label: 'Edycja masowa',
        s9l_edit: 'Zaznacz wiersze w siatce, wybierz jedną kolumnę i nową wartość, obejrzyj podgląd, a następnie zatwierdź. Obsługiwane przez <code>api/mass_edit.php</code> (<code>mass_edit_preview</code> / <code>mass_edit_apply</code>); tabela i kolumna są walidowane względem <code>spw_config.schema</code>, a aktualizacja wykonuje się jako zapytanie parametryzowane.',
        s9l_dup_label: 'Masowe duplikowanie / usuwanie',
        s9l_dup: 'Ten sam endpoint obsługuje też <code>mass_duplicate</code> (klonowanie zaznaczonych rekordów) i <code>mass_delete</code> (ich usunięcie) — zawsze wyłącznie w zakresie jawnie zaznaczonych identyfikatorów.',
        s9l_clean_label: 'Szybkie czyszczenie danych',
        s9l_clean: 'Znajdź i zamień w jednej kolumnie jednej tabeli (<code>api/data_cleanup.php</code>). Opcje: ignorowanie wielkości liter, tylko całe słowa oraz ignorowanie znaków diakrytycznych. Podgląd pokazuje liczbę pasujących wierszy i przykładowe wartości, zanim <em>Zastosuj</em> je nadpisze.',
        s9l_role_label: 'Uprawnienia',
        s9l_role: 'Oba endpointy wymagają roli <code>editor</code> i poprawnego tokenu CSRF; rola viewer nie ma do nich dostępu.',
        s10c_head: '10c. Rekordy użytkownika',
        s10c_desc: '<strong>Zarządzanie danymi → Rekordy użytkownika</strong> konfiguruje panel „Moje rekordy”, który każdy użytkownik otwiera z menu awatara — listę rekordów aktualnie do niego przypisanych we wszystkich tabelach. Zapisywane w <code>spw_config.user_records</code>.',
        s10c_map_label: 'Zakładka Mapowanie kolumn',
        s10c_map: 'Dla każdej tabeli zaznacz kolumny sklejane w etykietę przypisanego rekordu. Tabela bez zaznaczenia korzysta automatycznie z pierwszej kolumny siatki.',
        s10c_global_label: 'Zakładka Ustawienia globalne',
        s10c_global: 'Ile ostatnio przypisanych rekordów pokazywać na tabelę. Samo właścicielstwo pochodzi z <code>spw_record_owners</code> (patrz sekcja 9d).',
        s10d_head: '10d. Komentarze do rekordów',
        s10d_desc: 'Komentarze przypinane do dowolnego rekordu, przechowywane w <code>spw_comments</code> i obsługiwane przez <code>api/comments.php</code>. Nie wymagają osobnej zakładki w panelu — są dostępne dla każdej tabeli ze schematu.',
        s10d_where_label: 'Gdzie się pojawiają',
        s10d_where: 'Jako zakładka Komentarze w formularzu edycji i jako licznik w siatce danych. Menu awatara udostępnia też „Moje komentarze” — wszystko, co napisał bieżący użytkownik, od najnowszych, z odnośnikiem do rekordu.',
        s10d_perm_label: 'Zasady',
        s10d_perm: 'Treść jest ograniczona do 4000 znaków. Usunięcie komentarza to miękkie usunięcie (znacznik czasu), więc ślad audytowy pozostaje; rola viewer może czytać komentarze, ale nie dodawać.',
        s10e_head: '10e. Notatki prywatne',
        s10e_desc: 'Notatnik każdego użytkownika, przechowywany w <code>spw_notes</code> i obsługiwany przez <code>api/notes.php</code>. Otwierany pozycją <strong>Notatki</strong> w menu awatara. Podobnie jak komentarze, nie wymaga żadnej konfiguracji w panelu administracyjnym.',
        s10e_private_label: 'Ściśle prywatne',
        s10e_private: 'Każde zapytanie jest ograniczone do wywołującego użytkownika — notatki nigdy nie są współdzielone i żadna rola nie odczyta notatek innego konta. To odróżnia je od komentarzy, które są przypięte do rekordu i widoczne dla każdego, kto może go otworzyć.',
        s10e_link_label: 'Powiązanie z rekordem',
        s10e_link: 'Notatka może opcjonalnie wskazywać rekord (tabela + id), więc da się z niej wrócić wprost do tego, czego dotyczy. Może też istnieć samodzielnie.',
        s10e_reminder_label: 'Przypomnienia',
        s10e_reminder: 'Notatka może mieć <strong>datę przypomnienia</strong>. <code>cron/cron_notifications.php</code> wybiera notatki, których termin nadszedł lub minął, i dostarcza je jako powiadomienia w aplikacji pod ikoną dzwonka, więc przypomnienie ustawione na wcześniejszą godzinę tego dnia i tak zadziała przy uruchomieniu crona. Treść jest ograniczona do 4000 znaków, a usunięcie jest miękkie — dokładnie jak dla komentarzy.',
        sUpg_head: '17. Aktualizacja OpenSparrow',
        sUpg_desc: 'Po pobraniu nowej wersji sprawdź oczekujące migracje wydania w <strong>System → Migracje</strong>. Nagłówek panelu pokazuje żółte powiadomienie, gdy wymagane jest działanie.',
        sUpg_flow_head: 'Procedura aktualizacji',
        sUpg_step1: 'Pobierz nowe wydanie: <code>git pull</code>',
        sUpg_step2: 'Otwórz Panel → <strong>System → Migracje</strong>.',
        sUpg_step3: 'Najpierw uruchom <strong>Zastosuj oczekujące migracje</strong> (zmiany schematu bazy).',
        sUpg_step4: 'Przejdź do sekcji <strong>Migracje wydań</strong>. Zaznacz akcje do wykonania i kliknij <strong>Zastosuj zaznaczone</strong>.',
        sUpg_step5: 'Sprawdź, czy powiadomienie o aktualizacji zniknęło z nagłówka.',
        sUpg_what_head: 'Co robią migracje wydań',
        sUpg_what1: '<strong>Usunięcie pliku</strong> — przenosi nieaktualny plik do <code>storage/migrations_backup/&lt;wersja&gt;/</code>, a następnie usuwa go z katalogu roboczego.',
        sUpg_what2: '<strong>Usunięcie klucza konfiguracji</strong> — usuwa przestarzałe pole z klucza w magazynie <code>spw_config</code>; poprzednia wartość trafia najpierw do <code>spw_config_log</code>. Pomijane, jeśli administrator zapisał tę konfigurację w trakcie migracji.',
        sUpg_what3: '<strong>Przestarzałe (tylko informacja)</strong> — brak działania; plik nadal istnieje, ale przy użyciu generuje ostrzeżenie w <code>storage/logs/deprecations.log</code>.',
        sUpg_backup_head: 'Kopie zapasowe',
        sUpg_backup: 'Usuwane pliki są kopiowane do <code>storage/migrations_backup/&lt;wersja&gt;/</code> przed skasowaniem. Klucze konfiguracji żyją w bazie, więc ich poprzednia wartość zostaje w śladzie audytowym <code>spw_config_log</code>. Historia zastosowanych akcji w zakładce Migracje pokazuje lokalizację kopii dla każdej z nich.',
        sUpg_add_head: 'Dodanie wpisu migracji (dla współtwórców)',
        sUpg_add: 'Każdy pull request usuwający plik lub klucz konfiguracji musi w tym samym PR dodać wpis do <code>config/migrations.json</code>. W <code>removed_config_keys</code> pole <code>file</code> wskazuje klucz <code>spw_config</code> — dawna pisownia <code>schema.json</code> jest nadal akceptowana i mapowana na klucz <code>schema</code>. Przykład:',
        s14_head: '14. Automatyzacje',
        s14_desc: 'Silnik automatyzacji oparty na regułach. Definiuj wyzwalacze, warunki i akcje wykonywane po stronie serwera przy każdym tworzeniu, aktualizacji lub usunięciu rekordu. Reguły można duplikować (tworzy wyłączoną kopię) oraz włączać/wyłączać bezpośrednio z listy, klikając znacznik statusu.',
        s14_trigger_label: 'Wyzwalacze',
        s14_trigger: 'Trzy zdarzenia: <strong>After create</strong> (INSERT), <strong>After update</strong> (PATCH) i <strong>After delete</strong> (DELETE). Reguły delete widzą rekord w stanie sprzed usunięcia; akcja <strong>Update fields</strong> jest tam niedostępna, bo wiersz już nie istnieje.',
        s14_cond_label: 'Warunki (zagnieżdżone AND / OR)',
        s14_cond: 'Buduj wielopoziomowe drzewa warunków. Każda grupa ma przełącznik AND / OR. Kliknij <strong>+ Group</strong>, aby dodać podgrupę. Obsługiwane operatory: <code>equals</code>, <code>not equals</code>, <code>contains</code>, <code>not contains</code>, <code>is empty</code>, <code>is not empty</code>, porównania <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code> (najpierw liczbowo, potem daty, na końcu tekstowo), a dla zdarzeń update także detekcja zmiany: <code>changed</code>, <code>not changed</code>, <code>changed from</code>, <code>changed to</code> — porównują wartość sprzed i po zapisie. Przy zdarzeniu create stara wartość nie istnieje, więc <code>changed</code> pasuje, a <code>changed from</code> nigdy.',
        s14_actions_label: 'Typy akcji',
        s14_actions: '<strong>Update fields</strong> — ustaw dowolną kolumnę na wyzwalającym rekordzie. <strong>Send notification</strong> — wyślij powiadomienie do jednego lub wielu użytkowników (wybierz z listy; "Current user" = użytkownik który wywołał zdarzenie). <strong>Create record</strong> — wstaw nowy wiersz w dowolnej tabeli schematu. <strong>Send webhook</strong> — wyślij ładunek JSON (ID reguły, zdarzenie, tabela, ID rekordu, zmapowane pola — lub pełny rekord przy pustym mapowaniu, plus <code>old_data</code> przy zdarzeniach update i delete) na zewnętrzny endpoint http(s) metodą POST, PUT, PATCH lub DELETE. To jest ścieżka integracji z n8n, Make i własnymi odbiorcami: wskaż URL węzła Webhook w n8n. Opcjonalny sekret dodaje nagłówek <code>X-Sparrow-Signature</code> (HMAC SHA-256 treści), dzięki któremu odbiorca może zweryfikować pochodzenie; jest przechowywany zaszyfrowany i nigdy nie wraca do przeglądarki — zostaw pole puste, aby zachować zapisaną wartość, lub użyj <strong>Clear secret</strong>, aby ją usunąć. Własne <strong>nagłówki</strong> przenoszą uwierzytelnienie odbiorcy (np. poświadczenie Header Auth w n8n). Nazwy nagłówków pozostają widoczne, ale ich wartości są przechowywane zaszyfrowane i nigdy nie wracają do przeglądarki — zostaw wartość pustą, aby zachować zapisaną, usuń wiersz, aby ją skasować; zmiana nazwy nagłówka czyści jego wartość. <code>Content-Type</code>, <code>User-Agent</code>, <code>Host</code> i <code>X-Sparrow-Signature</code> są zarezerwowane. W polu <strong>On failure</strong> można włączyć 1 lub 2 ponowienia z odstępem — dotyczą wyłącznie timeoutów oraz odpowiedzi 5xx/429, nigdy 4xx, a ponieważ wysyłka jest synchroniczna, każde ponowienie wydłuża zapis użytkownika. <strong>Send email</strong> — zakolejkuj e-mail (odbiorcy, temat i treść obsługują szablony) w <code>spw_automation_emails</code>; wysyłkę realizuje <code>cron/cron_notifications.php</code> i wymaga <code>AUTOMATION_EMAIL_FROM</code>. Webhook i e-mail respektują własność rekordu na tabelach <code>owner_restricted</code> i trafiają do logu audytowego. Wszystkie wartości obsługują zmienne szablonu.',
        s14_vars_label: 'Zmienne szablonu',
        s14_vars: '<code>{{ current_user.id }}</code> — ID użytkownika który wywołał zdarzenie. <code>{{ record.POLE }}</code> — wartość dowolnej kolumny z wyzwalającego rekordu. <code>{{ old_record.POLE }}</code> — wartość kolumny sprzed aktualizacji (pusta przy zdarzeniu create). <code>{{ today }}</code> — dzisiejsza data (RRRR-MM-DD); działa też w wartościach warunków, np. deadline <code>&lt;=</code> <code>{{ today }}</code>.',
        s14_n8n_label: 'Połączenie z n8n (lub Make / dowolnym odbiorcą HTTP)',
        s14_n8n_intro: 'Akcja <strong>Send webhook</strong> to ścieżka integracji wychodzącej. OpenSparrow wypycha zdarzenia rekordów do odbiorcy; po żadnej ze stron nie jest potrzebna dodatkowa usługa ani wtyczka.',
        s14_n8n1: 'W n8n dodaj węzeł <strong>Webhook</strong>, ustaw metodę <code>POST</code> i skopiuj jego produkcyjny URL.',
        s14_n8n2: 'W OpenSparrow utwórz regułę na wybranej tabeli, wskaż zdarzenie (<strong>After create</strong>, <strong>After update</strong> lub <strong>After delete</strong>) i dodaj warunki.',
        s14_n8n3: 'Dodaj akcję <strong>Send webhook</strong>, wklej URL z n8n i zostaw mapowanie ładunku puste, aby otrzymać cały rekord — albo zmapuj pojedyncze klucze JSON na szablony.',
        s14_n8n4: 'Uwierzytelnienie — wybierz jedno: ustaw <strong>Secret</strong> i weryfikuj <code>X-Sparrow-Signature</code> w n8n (węzeł Crypto liczący HMAC SHA-256 z surowej treści, porównanie węzłem IF), albo dodaj <strong>nagłówek</strong> odpowiadający poświadczeniu <em>Header Auth</em> w n8n. Można użyć obu naraz.',
        s14_n8n5: 'Zapisz, zmień rekord i otwórz <strong>History</strong> — status <code>ok</code> oznacza, że odbiorca odpowiedział kodem 2xx.',
        s14_n8n_payload: 'Kształt treści: <code>{ rule_id, event, table, record_id, triggered_by, data }</code>, a przy zdarzeniach update i delete dodatkowo <code>old_data</code> ze stanem sprzed zmiany — wystarczy do policzenia różnicy bez trzymania własnej kopii po stronie n8n.',
        s14_n8n_warn_label: 'Ryzyko pętli:',
        s14_n8n_warn: 'jeśli przepływ n8n zapisuje z powrotem do tej samej tabeli przez API, ten zapis ponownie odpali reguły. Zawęź regułę warunkiem (np. polem statusu, którego przepływ nie dotyka), aby nie mogła wyzwolić samej siebie.',
        s14_history_label: 'Historia uruchomień',
        s14_history: 'Kliknij <strong>History</strong> przy regule, aby wyświetlić ostatnie 100 logów. Status: <code>ok</code> / <code>error</code> / <code>skipped</code> (warunki niespełnione). Logi przechowywane w <code>spw_automation_runs</code>.',
        s14_note_label: 'Uwaga',
        s14_note: 'Automatyzacje wykonywane są synchronicznie przy każdym pasującym zapisie — webhook czeka do 5 s na połączenie i 10 s na odpowiedź, a każde włączone ponowienie dokłada kolejną taką próbę wraz z odstępem. Błędne akcje są logowane, ale nie cofają oryginalnej zmiany rekordu.'
    }
};
